# 更新日志

记录本项目的实质性变更。分支工作流：开发在 `YinnnH_feature`，PR 到 `main` 后用 **merge commit** 合并 —— 不用 squash，因为 squash 会生成新 SHA，让特性分支与 `main` 分叉，进而导致 PR 重复显示已合并的文件、同一区域再改就冲突、每轮都要 force push —— 最后部署到服务器。

---

## 2026-09-19

### 一、炫动色域随等级递增：1 级 ±5°/±8%，499 级 ±100°/±20%（PR #44）

#### 动机

原来 1~499 级的炫动幅度是**写死的** —— 色相左右各偏 ±8°、明度上下各偏 ±14%（`level.ts` 里 `SHIMMER_HUE_DEG` / `SHIMMER_LIGHT_PCT`）。用户要「幅度随等级变」：低等级只微微颤动，高等级颜色大幅流动，让「等级越高越炫」不止体现在**速度**上，也体现在**幅度**上。

#### 改动

- 把两个常量换成**端点常量**：`LV1_HUE_DEG=5` / `LV499_HUE_DEG=100`、`LV1_LIGHT_PCT=8` / `LV499_LIGHT_PCT=20`，中间按等级**线性插值**（与速度共用同一个进度 `t = (lv-1)/(MAX_LEVEL-2)`）
- `shimmerGradient` 从「读模块常量」改成「接收 `hueDeg` / `lightPct` 两个参数」，由 `levelShimmer` 算好传入
- 徽章底色与称号文字沿用**同一套幅度**（称号仍是 0.72 加深版，只是底色更暗，幅度不变）—— 没有给称号单独调，保持既有「两者共享常量」的行为

插值结果（两端点）：

| 等级 | 色相偏移 | 明度偏移 |
|---|---|---|
| 1 | ±5° | ±8% |
| 250 | ±52.5° | ±14% |
| 499 | ±100° | ±20% |

499 级深红 `hsl(0, 74%, 42%)` 偏移 ±100° 后，色相扫过 **260°（蓝紫）→ 0°（红）→ 100°（绿）**，共 200°；1 级绿只扫过 **137° → 147°**，共 10°。

#### 验证

- `npm test` **31/31 通过**（新增一条测试，断言 1 级与 499 级的单侧色相/明度偏移命中端点值，且 250 级严格夹在中间）
- `npm run build` 通过；JS 哈希随之更新（`index-Bo4ke-g2.js` → `index-DN6pV_AH.js`），CSS 哈希未变（`index.css` 只改了注释，编译产物不含注释）
- 生成各等级炫动色条预览图（桌面 `炫动色域预览.png`），逐像素采样复核：1 级色相跨度 10°、250 级 105°、499 级 200°，与插值公式一致

#### 部署

- PR #44 以 merge commit 合入（`96b5b1e`）
- 前端部署：清空远程 `dist` → 上传 → **24/24 md5 逐条一致** → `docker compose up -d --build --force-recreate frontend`；回滚点是上一版 `803cd4c08e42`，新版 `todo-app:latest` = `181146729872`
- 线上验证（curl）：首页 200 且引用新 JS `index-DN6pV_AH.js`；新 `index-*.js` / `Stats-*.js` / `index-CEDRQXXM.css` / `manifest.json` / 图标全 200；旧 `index-Bo4ke-g2.js` 404（已随清空删除，无残留）
- 镜像保留：frontend 悬空 4 → 3（删最老的 `5e9d7c6f27ae`，逐个 `rmi` 不加 `-f`），backend 未动，redis 未受影响
- **验证边界**：没有在真机浏览器上确认高等级称号文字的实际可读性 —— 预览图是逐像素采样核对过的，但 499 级称号（0.72 加深 + ±20% 明度）在深色段的观感，等用户真机反馈
- **后续**：用户看完线上效果反馈「好难看」。同日第二期先收窄、再一路退，把幅度整块改回**写死的 ±8°/±14°**（即本期之前的值）—— 这一期的「幅度随等级递增」只活了几十分钟，且连方向一起被否掉。第三期又由用户点名重做了幅度（色相 400 级起 8°→30°、明度降到 ±6%），**幅度**这一路仍随等级变，但**499 级的上限从 ±100° 收到 ±30°**，且只有最后 99 级参与；第四期再把明度回调到 ±10%。详见后三节

---

### 二、炫动幅度走了一圈回到原点：改回写死的 ±8°/±14%，另加「进度条也炫动」

**这一期的正文是一次失败的尝试记录。** 真正的交付只有一条：等级进度条也加炫动。幅度那一块来回改了四版，全部被否，最后整块退回第十九期之前的写法。

#### 动机

第一期（同一天）把幅度改成随等级递增，499 级给到 ±100°/±20%。**用户看过线上效果后反馈「好难看」** —— 499 级色相扫过 200°，整条渐变糊成一片彩虹，反而看不出是「本等级的颜色」。同时用户要求：**等级进度条也要跟着炫动**（此前是纯色填充）。

#### 过程：四轮都没成

按用户提出顺序，每一轮都做了完整验证（单测 + 逐像素采样的预览图），每一轮都被否：

| # | 用户原话 | 做了什么 | 结果 |
|---|---|---|---|
| ① | 「好难看」 | 端点 100°→70°、20%→13% | 否 |
| ② | 「等级进度条也要相应炫动」 | 进度条复用徽章的渐变与时长 | **留下** |
| ③ | 「所有包含从资深到首席的对应等级，°都减去20°，明暗都减5%」→「还是太多了，再减30°」 | 251~400 级额外减最多 50°，两端渐入渐出 | 否 |
| ④ | 「先把明暗都都降到0」 | 明度偏移全局归零 | 否 |
| — | **「好难看，改回最初那版」** | 幅度整块退回写死的 ±8°/±14% | 终 |

最终状态：`SHIMMER_HUE_DEG = 8` / `SHIMMER_LIGHT_PCT = 14`，**1~499 级一个样**，只有速度仍随等级递增。`LV1_*` / `LV499_*` 四个端点常量、`TRIM_*` 与 `trimWeight()`、以及 `shimmerGradient` 的 `hueDeg` / `lightPct` 两个参数**全部删除**，`level.ts` 的炫动部分与 `2b4dada` **功能上逐字节等价**（`git diff 2b4dada -- frontend/src/level.ts` 只剩注释差异）。

#### 为什么整段删掉而不是留成「可配置」

③④ 当时把端点常量与收窄机制**留着**，理由是「用户说的是『先』，留个口子」。结果是：口子留着，方向却是错的，下一轮又在这套机制上继续调参，多花了两轮。**方向被否掉时应该把机制一起删干净** —— 留着的「口子」会诱使下一轮继续在同一个方向上打转。真要恢复，`git show 2b4dada:frontend/src/level.ts` 就是。

#### 为什么进度条不是「另配一套更淡的渐变」

用户的原话是「等级进度条**也**要相应炫动」——「相应」即同一个等级色。进度条与徽章在视觉上属于同一张卡片，如果各自跑不同相位/不同幅度的渐变，会看起来像两条互不相干的动画。所以直接复用 `badgeImage`，代价是两处必须同步改（这也是为什么渐变算在 `level.ts` 里、而不是各组件自己拼）。

#### 教训：方向不对时，验得再细也没用

四轮里每一轮的实现都是对的 —— 台阶问题、双谷问题、量化误差、采样点错误，都发现并修掉了，预览图逐像素采样与公式最大差 0.45°。**但方向本身是错的**，所以这些正确性一点没转化成价值。

更值得记的是：**④ 那一版把 258~346 级做成了彻底静止**（色相与明度都减到 0），这直接违反了第十九期定的「1~499 级全都炫动」。当时把它当成「用户明知后选的」写进了文档，但**更该做的是把这个冲突本身当成否决信号** —— 一个需要靠「用户已知情」来辩护的设计，通常就是该停下来的地方。停在这里能省掉后面整整两轮。

#### 验证

- `npm test` **32/32 通过**（删掉 4 条针对「幅度随等级变」「收窄段」「明度归零」「静止区间」的测试，新增 1 条护栏）
  - 新增护栏测试「炫动幅度写死 ±8°/±14%，所有等级一个样」：逐级断言 1/2/100/250/251/300/346/400/401/499 十级的单侧偏移都是 ±8°/±14%，且相邻等级之间**没有任何幅度变化**；另断言 1/250/300/499 四级都还在动（没有「完全静止」的等级）
    - 这条是防止哪天又悄悄把幅度改成随等级变的守卫。前面的注释里写明了缘由
  - 保留 DOM 测试：种子 2 小时（Lv.8，进度 ≈0.69），断言进度条填充的 `animationDuration` / `backgroundImage` 与徽章**逐字相等**、且宽度仍是百分比
    - 这条相等断言有「空对空」的风险（jsdom 若丢弃渐变，两边都是 `""` 也会通过），所以另加了一条 `toContain("linear-gradient")` 兜底 —— 实测 jsdom 保留了渐变，不是空对空
- `npm run build` 通过；JS 哈希 `index-BMIG_fWf.js`，产物 168.17 → **167.98 kB**（删掉收窄那套机制后小了 0.19 kB）
- `git diff 2b4dada -- frontend/src/level.ts` 显示**无功能性差异**，只剩注释 —— 这是「真的退回去了」的硬证据
- `index.css` 里那句为收窄段写的注释（「251~400 那三档额外收窄 20°/5%…」）已随之改回「幅度固定 ±8°/±14%，只有速度随等级递增」
- 生成预览图（桌面 **`炫动色域预览6.png`**：上半是幅度-等级曲线，**一条水平直线**；下半是 17 个等级色条 + 四张等级卡片示意，每张卡片的进度条都跟着同一条渐变走），**逐像素采样**复核：17 个采样级的色相/明度跨度与公式逐一对拍，全部命中 ±8°/±14°
  - 采样器本身踩过一次坑：色条是 `lo→mid→hi→mid→lo` 五站跨满宽，mid 在 `px=(w-1)/4` 处。曾取 `w//3`，落到 f≈1.33 的位置，量出来一律偏大约 1.33 倍（17 个点全「不符」）—— 是采样点错了，不是图错了

#### 验证边界

- **没有在真机上看过效果。** 幅度是写死的常数、又逐像素对拍过，数字上没什么可怀疑的；但「回到 ±8°/±14° 是不是就顺眼了」只能由用户看线上效果给出 —— 这一整天的来回正是因为判断只能靠眼睛
- 预览图是对 `level.ts` 逻辑的**复刻**（Python 重写一遍同样的插值与 HSL 往返），不是浏览器截图。像素采样证实两者一致，但复刻终究不是渲染本身；真实渲染还要经过 CSS 渐变插值与 `background-size:200%` 的滚动，那部分没在图上体现
- 这一版**没有**回退上一期之前的任何其它东西：速度仍随等级递增（1 级 20s / 499 级 1.145s），色相锚点、称号字体、字号全部未动。改的只有幅度

#### 部署

**与第三期一起，2026-09-19 12:26 上线**（同一个 PR #46、同一次部署，细节见第三期的部署段）。这一期本身停在 PR 等确认，未单独部署；幅度值随后被第三期覆盖，所以线上跑的不是这里的 ±8°/±14°。留着它是因为「整块回退」这个动作本身值得记 —— 当天别的版本都在某个方向上继续调参，只有它是**否定性**的：整块退回去，不留口子。

---

### 三、明度降到 ±6%，色相 400 级起从 ±8° 升到 ±30°

#### 动机

用户原话：「正负14全改成正负6，8°从250级开始增加，到499级为正负20°」。第一期那套「幅度随等级递增」虽然被否，但**明度那一路确实是降下来才对**——第二期回到 ±14% 只是回到原点，不是结论。这一期按用户给的数重做。

**看过 `炫动色域预览7.png` 后，用户把口径又推了一档**：「以400级为起点，到499级为30°」。也就是——起点从 250 级推到 **400**，终点从 20° 提到 **30°**。**改动区间从 249 级压缩到 99 级，跨度却从 12° 涨到 22°**，斜率因此变成原来的约 4.6 倍（0.048 → 0.222°/级）。本节的数全部是这一版（预览图 8）。

#### 改动

- **明度**：`SHIMMER_LIGHT_PCT` 14 → **6**，**所有等级一个样**（不随等级变）
- **色相**：400 级及以下**恒定 ±8°**，401~499 线性升到 **±30°**。新增 `HUE_FLAT_UNTIL` / `HUE_FLAT_DEG` / `HUE_MAX_DEG` 三个常量与 `hueDeg(level)`
  - 斜率 = 22° / 99 级 ≈ **0.222°/级**，只有最后 99 级在爬 —— 一段更陡但更短的坡
  - `shimmerGradient` 从「读模块常量」改成接收 `hue` 参数，由 `levelShimmer` 调 `hueDeg` 算好传入；徽章与称号共用同一个值
- 速度、色相锚点、称号字体、字号**一律未动**

| 等级 | 色相 | 明度 |
|---|---|---|
| 1 ~ 400 | ±8° | ±6% |
| 420 | ±12.4° | ±6% |
| 450 | ±19.1° | ±6% |
| 499 | ±30.0° | ±6% |

#### 为什么这次的「幅度随等级变」不是重蹈覆辙

第二期的教训写的是「**不要再自作主张**让幅度随等级变」。这一版仍然是「幅度随等级变」，但两者不是一回事：

| | 第一期（被否） | 本版 |
|---|---|---|
| 谁提的 | 我自己按「等级越高越炫」推的 | **用户点名的数** |
| 499 级幅度 | ±100°（色相扫过 200°，糊成彩虹） | **±30°**（扫过 60°） |
| 出现区间 | 1 级起，全程都在变 | **只有最后 99 级**（401~499） |
| 明度 | 8% → 20% | 恒定 6%（比原点还低一半多） |

第一期的毛病是**499 级的幅度绝对值太大**，不是「随等级变」这件事本身，也**不是斜率**。

这一点上一轮我写错了，这里更正：第二期与上一轮都把护栏写成了「不要再自作主张**把斜率放大**」，但算一下就知道站不住 —— 第一期的斜率是 `(100−5)/498 ≈ 0.191°/级`，而本版是 `22/99 ≈ 0.222°/级`，**比被否的那版还陡**。区间从 498 级压到 99 级，斜率自然会被推上去；拿斜率当护栏，等于要求「要么别缩短区间，要么别抬上限」，两条都是用户点名要的。

真正的护栏是另外两条：**别再抬高 `HUE_MAX_DEG`**、**别再让平段（`HUE_FLAT_UNTIL`）缩短**。这两条一旦松掉，才会退回第一期那个「扫过 200° 糊成彩虹」的状态。`level.ts` 的注释已按这个口径重写。

顺带一个观察：起点从 250 推到 400、终点从 20 提到 30，说明**预览图确实起了作用**——用户是看着 7 号图中那条从 250 级就开始缓缓爬升的线，判断出爬升来得太早、幅度又不够，才有了这一版的收窄区间 + 抬高终点。

#### 验证

- `npm test` **33/33 通过**。第二期那条护栏「幅度写死 ±8°/±14%，所有等级一个样」拆成两条：
  - 「明度偏移所有等级一律 ±6%，不随等级变」：10 级抽查 + 逐级断言相邻等级没有明度差
  - 「色相偏移 400 级及以下恒为 ±8°，401~499 线性升到 ±30°」：**平段逐级遍历全部 400 级**、终点 499、中段 450 级 ≈19.11°、边界 400→401 无台阶、401 起逐级单调不降且每级增量 < 0.4°，外加「没有完全静止的等级」
    - 平段改成逐级遍历（上一版只是抽查 6 个点）—— 400 级这个边界离常见的测试点很远，抽查容易漏
    - 单调那条第一版写的 `toBeGreaterThanOrEqual` 会**假挂**：幅度本身严格单调，但两个停靠点各自被序列化到 1 位小数，反解回来的差值带约 1e-13 的浮点噪声（实测 `8.299999999999955` vs `8.300000000000011`），卡死 ≥ 就被这点噪声判负。改成 `toBeGreaterThan(prev - 0.11)`（容差 = 一个量化步长）
- `npm run build` 通过；JS 哈希 `index-BMIG_fWf.js` → `index-CWSPHMbY.js` → `index-DBDM7o5L.js`，产物 167.98 → **168.08 kB**（两版体积相同，只是常量变了）
- 预览图（桌面 **`炫动色域预览8.png`**）：上半两个面板 —— ① 色相（400 级前是平段，之后爬升；灰色幽灵线是上一版的「250 级起 8°→20°」）、② 明度（6% 平线 vs 最初的 14%）；下半 12 个等级色条 + 4 张等级卡片
  - 与 v6 一样**先从 `level.ts` 经 esbuild 导出真实值再对拍**：499 级的 badge / title / durationS **逐字一致**才出图
  - 逐像素采样复核：12 个采样级（覆盖 400/401 边界）的色相跨度与公式最大差 **0.24°**，明度跨度处处 **6.0%（最大偏差 0.12%）**，首尾像素分量差 ≤2（无缝循环仍成立）

#### 验证边界

- **没有在真机上看过效果。** 具体数值是用户直接给的，实现与公式的一致性已用两种独立方式（逐字对拍 + 像素采样）确认；但「只在最后 99 级爬 22°，这个变化在高等级区间够不够看得出来」「降到 6% 会不会太静」只能由眼睛判断
- 预览图只画到 499 级 —— **500 级（满级）走的是另一套 `.rainbow-*` 彩虹，不在这些数里**，所以「499 → 满级」那一步的颜色跳变没有在图上体现
- 预览图仍是 `level.ts` 逻辑的复刻而非浏览器截图，真实渲染还要经过 CSS 渐变插值与 `background-size:200%` 的滚动
- PR #46 里叠着三个提交（第二期的「回退」、本期的「250 级起 8°→20°」、本期的「400 级起 8°→30°」），**都没单独上过线，是一起随这次合并上线的**。要单独看本节的最终口径，diff 只看最后一个提交即可；中间的 250/20 那版已被这一版覆盖，只留在提交历史里

#### 部署

2026-09-19 12:26，与第二期一起随 PR #46 上线。

- PR #46 以 **merge commit** 合入 `main`（`8c33e83`，两个父提交 `5b6a2ff` + `494e6af`，**不是 squash**）。本地 `main` 已同步
- 前端部署：`npm run build` → **清空远程 `frontend/dist`** → 上传 → **24/24 md5 逐条一致** → `docker compose up -d --build --force-recreate frontend`
  - 回滚点 = 上一版 `todo-app:latest` = `181146729872`；新版 = `abde572d1eea`
  - 本次**没有**上传 `nginx.conf` 与 `docker-compose.yml` —— 这一期只动了 `level.ts` / `level.test.ts` / `index.css` 的注释 / 文档，仓库里那两份文件未改动
- 线上验证（curl）：首页 200 且引用新 `index-DBDM7o5L.js`
  - **把 24 个产物逐个经 HTTP 取回、算 md5 与本地构建产物比对，逐条一致** —— 这比「首页能打开、引用了新文件名」强得多：验的是 nginx + 容器里**实际服务的那份字节**，而不是「磁盘上有这个文件」
    - 这条是 2026-09-18 那条后端教训的前端版：**「上传的产物是对的」与「跑起来的产物是那个」是两件事**。前端有哈希文件名，看着比后端安全，但镜像里那份是否真的换了，只有比内容才知道
    - 过程中先用错了文件名（拿上一轮的 `Stats-Bni-xDrw.js` 去验，得 404）—— 本轮 `Stats` 的 chunk 哈希随 `level.ts` 变了，实际是 `Stats-ClI72A4e.js`。**验证脚本里的文件名要现取现用，不能凭上一轮的构建输出写死**
  - 旧产物（`index-CWSPHMbY.js` / `index-BMIG_fWf.js` / `index-DUdhEDxJ.js` / `Stats-Bni-xDrw.js`）一律 404，无残留
- 镜像保留：frontend 悬空 4 → **3**（删最老的 `7361b64a684a`，逐个 `rmi` **不加 `-f`**）；backend 悬空正好 3 个，未动；`redis1/2/3` 状态未受影响（Up 15 months）
- **验证边界：没有在真机上看过效果。** 这一整天的判断标准始终是「好不好看」，而那是眼睛的事 —— 部署验证只能证明「线上跑的就是这一版」，证明不了「这一版好看」
  - 尤其：**500 级（满级）走的是另一套 `.rainbow-*` 彩虹，不在这一期的数里**，499 → 满级那一步的颜色跳变没有任何验证
- **后续**：这一版上线后，用户看过线上效果，判断 6% **偏静**，当天第四期把明度回调到 ±10%（色相那一整套不动）。本节「6% 会不会太静」这个悬着的问题，答案由用户给出了：太静。详见下一节

---

### 四、明度从 ±6% 提到 ±10%

#### 动机

用户原话：「明暗在改成10%吧」。第三期把明度从 14% 降到 6% 是按用户点名做的，上线看过之后判断 **6% 偏静**，回调到 10%。

这一期**只动一个数**：`SHIMMER_LIGHT_PCT` 6 → **10**。色相那一整套（400 级前平段、401~499 升到 ±30°）、速度、锚点、字体、字号全部不动。

#### 明度这一路的完整轨迹

明度是当天被调得最勤的一条参数 —— 一天之内落到过六个不同的设定，最后还**回调**了一次（6 → 10）。完整轨迹：

| 值 | 出自 | 结果 |
|---|---|---|
| ±14% | 长期以来的默认值 | 第一期之前 |
| ±8% → ±20% | 第一期「幅度随等级递增」 | 否 |
| ±13% | 第二期端点收窄 | 否 |
| 0 | 第二期「先把明暗都降到 0」 | 否（且连带 258~346 级彻底静止） |
| ±14% | 第二期整块回退 | 否 |
| ±6% | 第三期用户点名 | 上线了，但偏静 |
| **±10%** | **本期** | 已上线（PR #48） |

明度**始终不随等级变**（除被否的第一期外），这一点从没动过；变的自始至终只是那个绝对值。

#### 验证

- `npm test` **33/33 通过**，只改了那条护栏的断言值（`toBeCloseTo(6)` → `toBeCloseTo(10)`）与标题
- `npm run build` 通过；JS 哈希 `index-DBDM7o5L.js` → `index-qK1EHlZ7.js`，产物 168.08 → **168.09 kB**
- 预览图（桌面 **`炫动色域预览9.png`**）：面板 ② 那条明度平线从 6% 抬到 10%，灰色幽灵线是刚上线的 6%；色相面板与色条、卡片一并重出
  - 仍**先从 `level.ts` 经 esbuild 导出真实值再逐字对拍**：499 级的 badge / title / durationS 一致才出图
  - 逐像素采样复核：明度跨度处处 **10.0%（最大偏差 0.00%）**，色相跨度与公式最大差 0.39°（未受影响），首尾像素分量差 ≤2

#### 验证边界

- **没有在真机上看过效果。** 这一轮改的就是「6% 看着太静」这个纯观感判断，而它只能由眼睛给出 —— 数值实现的一致性是确定的（明度处处 10.0%，像素级零偏差），但「10% 是不是刚好」要上线看
- 预览图仍是 `level.ts` 逻辑的复刻而非浏览器截图；同样**只画到 499 级**，满级的 `.rainbow-*` 不在其中

#### 部署

2026-09-19 12:39 上线。

- PR #48 以 **merge commit** 合入 `main`（`39c731d`，两个父提交 `e2937dc` + `92ee4e2`，**不是 squash**）。本地 `main` 与 `YinnnH_feature` 都已同步
- 前端部署：`npm run build` → **清空远程 `frontend/dist`** → 上传 → **24/24 md5 逐条一致** → `docker compose up -d --build --force-recreate frontend`
  - 回滚点 = 上一版 `todo-app:latest` = `abde572d1eea`（即第三期那版 ±6%）；新版 = `c073cd168e53`
  - 本次**没有**上传 `nginx.conf` 与 `docker-compose.yml` —— 这一期只动了 `level.ts` / `level.test.ts` / `index.css` 的注释 / 文档，那两份文件未改动
- 线上验证（curl）：首页 200 且引用新 `index-qK1EHlZ7.js`
  - **24 个产物逐个经 HTTP 取回算 md5，与本地构建产物 24/24 一致** —— 验的是容器**实际服务的那份字节**
  - 旧产物（`index-DBDM7o5L.js` / `index-CWSPHMbY.js` / `index-BMIG_fWf.js` / `Stats-ClI72A4e.js`）一律 404，无残留
  - `/api/todos` 未带 token 仍 **401**，后端与反代链路未受影响
- 镜像保留：frontend 悬空 4 → **3**（删最老的 `effc5790ff76`，逐个 `rmi` **不加 `-f`**）；backend 悬空正好 3 个，未动；`redis1/2/3` 状态未受影响（Up 15 months）
- **验证边界：以上只能证明「线上跑的就是这一版」，证明不了「这一版好看」。** 10% 与 6% 的取舍最终还是看真机观感 —— 这一天从 ±14% 一路调到 ±10%，每一次的依据都是眼睛，不是数字

---

### 五、待办事项支持拖拽手动排序（PC 直接拖 / 手机长按 1 秒）（PR #50）

#### 动机

在这之前，列表顺序**完全由创建时间决定**，用户改不了。这条链路上原本没有一环是可调的：`TodoRepository` 的派生查询名 `findByUserIdOrderByCreatedAtAsc` 是 UI 顺序的唯一来源，`Todo` 实体没有顺序字段，前端 `TodoList` 拿到的数组也没排过 —— 谁先建谁在上面。

要求是**跨设备同步**的手动排序。本项目的业务数据一律存后端、前端只留登录态，所以这必然是一次前后端都动的改动，而不是纯前端排序。

用户明确的两条交互要求：电脑端鼠标**直接拖**（不用长按）；手机端没有鼠标，必须**长按 1 秒**才浮起来，以免和滚动列表打架。另外要有「往上浮一点 + 阴影」的拖拽反馈。

#### 改动 · 后端

- `Todo` 加可空字段 `sortOrder`（列名 `sort_order`，走 Hibernate 默认命名策略，未写 `@Column(name=…)`）。`ddl-auto: update` 会自动 `ADD COLUMN`，MySQL 8 走 INSTANT
- 列表查询改为 `findByUserIdOrderBySortOrderAscCreatedAtAsc`。**`createdAt` 这个次级排序不是装饰**：加列时已有行的 `sort_order` 全是 NULL，撞号时也要有个确定的次序，否则列表会随机跳动
- 新增 `PUT /api/todos/order`，body `{ parentId, orderedIds }`，返回 204。`parentId` 为 null 表示排顶层，非 null 表示排某个合集的子任务
- `create()` 里新事项取该组 `max(sortOrder) + 1`，**沿用「新事项追加到末尾」的既有行为**
- 新增 `TodoOrderBackfill`（`ApplicationRunner`）：启动时把 `sortOrder IS NULL` 的行按 `(userId, parentId)` 分组、组内按 `createdAt` 升序赋 `0..n-1`。幂等 —— 补完之后没有 NULL 了，再启动就是空转
- **刻意不改 `TodoDto`**：前端渲染顺序完全来自数组顺序，数值一个地方都用不到。不改的连带好处是老前端跑在新后端上完全正常（新列它看不见），前后端不必同时上线

#### 改动 · 前端

- 引入 **dnd-kit**（`@dnd-kit/core@6.3.1` + `@dnd-kit/sortable@10.0.0`）—— 这是本项目第一个第三方 UI 运行时依赖
- 传感器靠 Mouse / Touch **天然分流**，正好对上「PC 直接拖 / 手机长按 1 秒」：
  - `MouseSensor` + `distance: 4` —— 鼠标移动 4px 才算拖，纯点击（勾选、双击编辑）不触发
  - `TouchSensor` + `delay: 1000, tolerance: 10` —— 按住 1 秒才算，期间手指移动超过 10px 中止激活。**这条是列表还能正常滑动的关键**
- 一级列表与每个合集的子任务列表各一个 `SortableContext`，共用**同一个** `DndContext`（官方支持嵌套 `SortableContext`，只要两层 id 不重 —— 本项目是 UUID，天然不重；嵌套 `DndContext` 则不行，内层会和外层同时收到同一批事件）
- 自定义 `collisionDetection`：按 active 项的 `parentId` 过滤掉其它分组的候选，**跨层自然落空**。顺带规避了多容器下 `closestCenter` 的碰撞闪烁（等距时在两个容器间来回跳，dnd-kit#1213）
- `<DragOverlay>` 做浮起副本 + `index.css` 新增 `@keyframes drag-lift`（`scale(1.02) translateY(-4px)` + 阴影由小变大，140ms）
- 新增 `frontend/src/reorder.ts` 纯函数：`siblingIds`（同父兄弟的 id 顺序）、`applyGroupOrder`（把某组重排，其它元素位置不动）
- `store.tsx` 新增 `reorderSiblings`：**先乐观更新本地 `todos`**（拖拽反馈要跟手），再调接口，失败回滚 + 报错

#### 几个刻意的决定，以及为什么

| 决定 | 理由 |
|---|---|
| 筛选视图下用 `arrayMove` 作用在**完整**兄弟列表上，而不是「把可见子集的新顺序合并回完整顺序」 | dnd-kit 的拖拽语义本来就是「邻居让位」而非「两者对调」；一行 `arrayMove` 顶掉了一个纯函数 + 一组单测。**但下标必须自己在完整列表上算** —— 库给的 `sortable.index` 是可见子集的下标，筛选视图下拿它当完整列表的下标一定会错 |
| `setNodeRef` 挂 `<li>`，`listeners` 挂内层卡片 `div` | 测量单位得是整行（卡片 + 子任务列表），否则邻居的让位距离只按卡片高度算，带子任务的合集落地时会再跳一截。而 `listeners` 若跟着挂 `<li>`，按子任务会同时命中内外两层激活器（dnd-kit 只保留一个 active），结果是「拖子任务变成拖整个合集」 |
| **不** spread `useSortable` 返回的 `attributes` | 它默认带 `role="button"` / `tabIndex=0`，会让卡片与内部的 checkbox、按钮形成嵌套交互内容，并给现有测试里的 `getByRole("button")` 塞进一批新候选。本方案也没接 `KeyboardSensor`，那些 aria 属性是无对应行为的空壳 |
| `TouchSensor` 的 `tolerance` 是必填 | 类型上 `DelayConstraint.tolerance` 没有问号，库内部直接把它喂给距离判断，缺了会解构 undefined 崩掉。文档说的「默认 5」在 6.3.1 的产物里没有实现 |
| `touch-action: manipulation` 而不是 `none` | 整张卡片都是拖拽面，`none` 会让用户在任何一张卡片上都无法滑动页面。`manipulation` 只关掉双击缩放，保留 panning |
| 浮起 keyframe 套在 overlay **内层再包一层**的 div 上 | 库靠行内 `transform` 让副本跟随指针，并会在放下时读回该节点的 `getComputedStyle().transform` 播下落动画；CSS 动画优先级高于行内样式，套在库定位的那个节点上会把这两件事一起顶掉 |
| 原项在拖拽期间 `opacity-0` 整个隐掉 | 用了 `DragOverlay` 之后原项**仍会被 strategy 挪到目标槽位**（`shouldDisplaceDragSource = !useDragOverlay && isDragging`），不隐就会看到「一个跟着手指、一个自己挪」的重影 |
| 长按成功时 `navigator.vibrate?.(10)` | 用户要的。部分 WebView（如百度 App）会屏蔽它，所以**它只是附加反馈，浮起动画才是主反馈**，拿不到就算了 |

#### 顺带修掉的两处

- **`reorder` 端点对重复 id 返回 204 而非 400。** `[A, A]` 会让 A 先拿位次 0 再拿 1，与别的项撞号，**静默打乱顺序** —— 而「顺序对不对」正是这个功能的全部价值。已加一道 `Set` 校验，重复即 400
- **仓库根目录残留的 `package.json` / `package-lock.json` / `node_modules`。** 上一轮会话把 dnd-kit 装到了仓库根而不是 `frontend/`（项目自 2026-09-18 起根目录不该有 `package.json`）。三个都是未跟踪文件，已删除并改装到 `frontend/`

#### 后续调整：拖合集时子任务自动收起

用户试过之后提的问题：**展开着的合集一拖起来就很难受** —— 卡片隐了，它下面的子任务还整整齐齐杵在原地跟着走。要求是拖拽时自动收起、放下后再展开（**要动画，和手点「收起」一样**），本来就收起的则不用管。

改动两处：`Collapsible` 的 `open` 在拖拽期间强制为 false；新增 `<RemeasureWhileCollapsing>`，在收起动画期间逐帧催 dnd-kit 重测。

**核心矛盾**：收起是 250ms 高度动画，这让被拖那一行逐帧变矮、后面所有行的位置跟着上移；而 **dnd-kit 只在「拖拽开始」和「droppable 增删」时重测各行矩形**（`useDroppableMeasuring`），**它不认节点变矮**。不处理的话，邻居的让位距离会一直按「还没收起」的旧矩形算 —— 比不收起还歪。

**中间试过一条更省事的路，否掉了**，记在这里免得将来有人再走一遍：拖拽期间把 `<Collapsible>` 整个**卸载**。那样几何确实立刻就对（子任务的 droppable 注销 → `Action.UnregisterDroppable` 换新 `DroppableContainersMap` → 触发全量重测），但子任务是**瞬间消失**的。用户明确要动画，所以最终走的是「照常动画 + 逐帧重测」。

**为什么库自己不会重测**（三个前提都在产物里实查过，不是猜的）：

- `defaultMeasuringConfiguration.droppable.frequency` 是**字符串** `'optimized'`（`core.esm.js:2485`），而定时重测那个 effect 开头就有 `typeof frequency !== 'number'` 的早退（`:2050`）—— 库**没有任何周期性重测**
- `MeasuringStrategy` 的三个值都只影响「什么时候**允许**测」（`isDisabled()`，`:2063`），不影响「多久测一次」。把默认的 `WhileDragging` 换成 `Always` 解决不了
- `droppable.measure` 是 `getTransformAgnosticClientRect`（即 `getClientRect(el, { ignoreTransform: true })`），它会**扣掉元素自身的位移 transform** —— 所以反复重测是**收敛**的，不会把让位的 transform 叠加进去。这条是这个方案能成立的前提

于是 `<RemeasureWhileCollapsing>` 在动画那 300ms 里逐帧调 `measureDroppableContainers([])`。**空数组才是全量重测**；传了 id 是「只测队列里的、其余复用缓存」，正好会跳过想测的那一行。另外类型声明把这个参数写成必填（`store/types.d.ts:81`）而实现里有 `ids === void 0 → []` 的兜底，两者对不上，所以显式写 `[]`。

**只跑动画那么长，不是整个拖拽过程**：每测一次都要重渲染整个列表，全程 60fps 地跑在低端机上会掉帧。启动条件也收窄了 —— 只有「被拖的那行确实有子任务、且当前是展开的」才启动。

另外确认过两件事：

- `useSortable` 只在 `items` 真变了才调 `measureDroppableContainers(items)`（`sortable.esm.js:313`）。我们的 `items` 数组拖拽期间不动，不会插进来抢
- 放下时的下落动画是**纯位移**：`adjustScale` 默认 `false`，`transform.scaleX/Y` 恒为 1，关键帧里 `active.rect.height` 不参与计算。所以落地瞬间子任务展开、这一行变高，**不会**把浮层拉变形

动画到 250ms 结束时内容会真正卸载，那一步本身又会触发一次全量重测 —— 所以动画收尾之后的矩形是必然正确的，逐帧重测只负责修「动画进行中」那段窗口。

改完重跑：`npm test` 45/45、`npm run build` 通过、产物 `@layer` 仍为 0。

#### 修掉的 bug：快速拖动后子任务永久收起

用真实浏览器逐帧录制时抓到的，**它是老代码里的坑，这次改动的形状把它放大成了必现**。

现象：**按下、浮起、随手一放（前后不到收起动画的 250ms），子任务收下去就再也不回来了** —— 行内 `height` 停在退场动画设的 `0px` 上，内容不可见，且没有任何报错。

根因在 `Collapsible` 的入场动画 effect：

```js
useEffect(() => { … }, [mounted]);   // 旧：只盯 mounted
```

收起动画走到一半又被展开时，`mounted` 从头到尾都是 `true`（卸载要等收起动画结束才发生），只盯它的 effect **根本不会重跑**；而退场 effect 已经把这个 div 的行内 `height` 设成了 `0px`，没人再改回来。

依赖改成 `[open, mounted]` 并在开头 `if (!open) return`，两个方向都能触发。**手点「收起」后立刻再点「展开」本来也会踩到同一个坑**，只是人手很难快到 250ms 以内 —— 拖拽让「收起」由机器触发，于是必现。

逐帧验证（真实 Edge + CDP，按住 120ms 后松开）：

```
旧：  …165 → 126 → 94 → … → 0 → 0 → 0 → … （永久停在 0）
新：  …165 → [松开] → 297px 行内高，实际 165 → 190 → 233 → 264 → 282 → 296 → 297 ✓
```

**这个 bug 单靠 `npm test` 抓不到**（jsdom 里 `getBoundingClientRect()` 与 `scrollHeight` 恒为 0，动画数值全退化成 0），只靠肉眼也只在拖得快时才出现。

#### 改动：子任务跟着卡片一起浮起来，边浮边收回

用户看完真机之后的最终形态。中间试错过一版（在列表原位留一个浅蓝「虚位框」，子任务收拢到它的底边），**被否掉了** —— 用户要的不是「收进原位置一个框」，而是：

> 浮起的时候，子任务和事项框**一起浮起到同一位置**，然后浮起时**边浮边收回**

也就是收起这件事要发生在**手里那个浮层**上，不是在列表原位。

改了两处：

- `SortableRow` 里把 `{children}` 挪进 `isDragging` 那层 `opacity-0` 的包裹 div —— 拖拽期间**整行一起隐掉**。子任务这会儿已经跟着浮层飘走了，原地再画一份就是重影。`listeners` 仍只挂在卡片那一层（挪到外面的话，按下子任务会同时命中内外两个激活器，变成「拖子任务 = 拖整个合集」）
- `DragOverlay` 里除卡片外再渲染一份子任务，包在新的 `OverlayFolded` 里 —— **挂上时从自然高度收到 0**，就是「边浮边收回」

`OverlayFolded` 没用现成的 `Collapsible`：后者的入场要等 `open` 从 false 翻到 true，而这里要的正好相反 —— 出生就是展开的、然后当场收掉，于是直接挂载时量一次 `scrollHeight` 最省事。只能收起不能展开，因为浮层一放下就整个卸载了，没有展开这回事。

本来就收起的合集不带浮层里那份子任务 —— 正好对上最初那条「如果子任务本身就是合起来的，则不用管」。

#### 验证

**后端端到端（本机一次性 MySQL 9.6 + 本地后端，未碰生产库、未碰本机已有的 MySQL 服务）**

- 正常路径：顶层重排 → `GET /api/todos` 顺序确实变了且子任务未受牵连；合集内子任务重排同理
- **新建落位**：重排之后新建的顶层事项落在**末尾**、新建子任务落在子任务末尾。这条专门验一个静默坑 —— 若用 `findByUserIdAndParentId(uid, null)` 取 max，SQL 里 `NULL = NULL` 恒为假、顶层永远查不到，新事项会插到列表**中间**且零报错
- 越权与不匹配，**6 种情形一律 400**：不存在的 id、别人的 id、顶层事项配了合集 `parentId`、子任务配 `null`、重复 id、真 id 混假 id。且这些请求之后顺序没有被改坏
- 鉴权：不带 token → 401；拿 u2 的 token 排 u1 的事项 → 400
- 空数组 → 204 且顺序无变化
- **回填**（把库退回升级前状态：`DROP COLUMN sort_order`、数据留着，再启动新版后端）：
  - `ddl-auto` 自动把列加回来；回填 **17/17 全中，`count(*) - count(sort_order) = 0`**，每组 `count(distinct sort_order)` 等于组内条数（无撞号）
  - 一个**从未手动重排过**的用户，升级前后顺序**逐位一致**（这是生产升级的实际保证）
  - 一个被手动重排过的用户，顺序回到创建顺序 —— 印证回填确实是「组内按 `createdAt`」而不是乱编
- 回归：`DELETE /api/todos/completed` 仍正常（它复用的那个查询被改过名），已完成合集连同子任务清掉、未完成的保留且顺序未乱

**前端**

- `npm test` **45/45 通过**（原有 33 条全部保留，新增 `reorder.test.ts` 9 条 + store 层 3 条：乐观更新、子任务只动组内、失败回滚）
- `npm run build` 通过（`noUnusedLocals` 会挡下残留变量）
- 产物自检：CSS 里 `@layer` **0 处**（Tailwind v3 硬约束）、`@keyframes drag-lift` 确实进了产物
- **Chromium 97（T7 内核）兼容性**：扫产物里的越界 API，`structuredClone`(98) / `Object.groupBy` / `.findLast` / `Object.hasOwn` / `AbortController` 全部 **0 处**；实际命中的只有 `ResizeObserver`(64) 与 `MutationObserver`，都在 97 以内。（另有一处 `reportError`，是 React 自带的功能探测兜底 `typeof reportError=="function" ? reportError : console.error`，不是 dnd-kit 引入，且缺它也会退化到 `console.error`）

#### 验证边界（重要）

- **没有在真实浏览器里拖过。** jsdom 里 `getBoundingClientRect()` 恒为 0，dnd-kit 的碰撞与让位全部退化成 0，硬测只会写出假绿的测试 —— 所以自动化只覆盖了纯函数与 store，**拖拽本身一行自动化都没有**
- 手机端「长按 1 秒 vs 列表照常滚动」这一对矛盾**只能真机判定**，且失败模式很隐蔽（`tolerance` 偏紧会变成「十次里成功三次」，用户只会觉得这功能时灵时不灵）
- 整卡可拖之后，双击编辑、勾选完成、开始/暂停计时、删除这几个操作是否还都正常，需要手工逐项过一遍
- 「拖起即收起、落地即展开」的**时机和手感**同样只能真机看。逻辑上重测量的是确定值（见上一节），但「收得突不突兀」「邻居让位对不对」是观感问题，自动化答不了
- 低端机上拖动是否掉帧没测（列表里每行都有炫动动画在跑，拖动时更吃性能）
- **尚未部署。** 后端新端点必须先上线，否则前端每次拖拽都会打到 404

#### 部署

**停在 PR #50，等用户先在本地 / 真机确认手感 —— 拖拽是交互改动，静态预览图证明不了任何事。** 顺序是**后端先、前端后**：后端的新列可空、DTO 不变，老前端跑在新后端上完全正常；反过来新前端跑在老后端上，每次拖拽都是 404 + 回滚。

上线后立刻要验的是回填完整性：

```sql
select parent_id, count(*), count(sort_order) from todos group by parent_id;
```

两个 count 必须相等（没有漏回填的 NULL）。

---

## 2026-09-18

### 一、删除待办加二次确认（PR #15 · `7efd000`）

#### 动机

删除按钮此前点了就执行，误触即不可逆。合集更是一删带走全部子集——时长虽不丢（见下），但要重建整棵结构很麻烦。

#### 改动

单个删除前加一层确认弹窗。`src/TodoList.tsx` +118 / −7，测试文件 +33 / −7。

**用自定义模态框而非 `window.confirm`**：原生弹窗样式不可控（百度 App 里弹的是系统对话框，与应用视觉割裂），也没法随应用一起改版。样式全走现有 Tailwind v3 工具类，未引入新依赖，Chromium 97 兼容。

弹窗只有两句：「确定删除吗？」「删除后不可恢复。」不显示事项名、不列子集数——确认框只需回答「删不删」，这些上下文点按钮时本来就知道。初版曾写上「已记录的计时时长会保留在统计中」，那是把确认框当成了讲道理的地方，已删。

| 决策 | 取值 | 理由 |
|---|---|---|
| 默认焦点 | 「取消」 | 避免一路回车误删 |
| 点遮罩 / 按 Esc | 等同取消 | 常规预期 |
| 待删项存储 | 只存 id | 弹窗不展示内容，存 id 足够确认时定位 |
| 「清除已完成」 | **不加**确认 | 本次范围只到单个删除 |

> 删待办其实并不丢时长：`TodoService.delete()` 对事项**及其每个子集**都调用 `endRunning()`，只补上会话的 `end`，记录本身保留。这条属于实现细节，不写进界面。

#### 验证

- `npm test` **18/18 通过**（原「删除任务」一条拆为「确认后才真删」+「取消则不删」两条）
- `npm run build` 通过（tsc + vite），CSS 产物 17.23 kB
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均为 **0**，v3 约束未被破坏
- **未做浏览器实测**：`TodoList` 需登录后才渲染，本地无后端，交互由测试覆盖

---

### 二、「子集」更名为「子任务」，子任务列表可收起（PR #17 · `f15f3f7`）

#### 动机

两件事一起做：

1. **「子集」这个词不好懂。** 界面上「合集」「子集」并列，都是集合论的词，但用户心里想的就是「这个任务下面还有几个小任务」。「子任务」是直觉词。
2. **子任务多了会把列表撑长。** 四门课还好，再多几门就得一直往下滚，需要一个收起来的开关。

#### 改动

**更名**：全仓库「子集」→「子任务」，11 个文件。

| 层 | 涉及 |
|---|---|
| 界面 | `TodoList.tsx` / `Stats.tsx` 的按钮、占位符、提示语 |
| 后端 | 报错消息（`子集下不能再加子集` 等，用户看得到） |
| 测试 | 与界面文案耦合的断言 |
| 注释 / 文档 | `src/` 各处注释、`README.md` |

`更新日志.md` 里 2026-09-17（PR #10）那条的「子集」**保持原样** —— 那是当时的记录，改写会让人以为功能当时就叫这个名字。更名这件事记在本期。

**收起 / 展开**：合集行右侧、`+ 子任务` 旁边加一个切换按钮。

| 决策 | 取值 | 理由 |
|---|---|---|
| 默认状态 | **展开** | 加子任务本就是为了给它们计时，点开就能点；折叠是主动收拾长列表的手段，不该是默认门槛 |
| 按钮位置 | 合集那一行，**不是**列表末尾 | 痛点就是列表长 —— 按钮得在不用滚动就能看到的地方 |
| 收起时点「+ 子任务」 | 自动展开 | 否则刚加的子任务看不见，等于操作没反馈 |
| 状态存放 | 内存，不持久化 | 纯视图偏好；刷新回到展开，与默认一致 |

收起后「N 个子任务」计数仍在计时行显示，用户依然知道里面有几个；正在计时的子任务也不会「消失」—— 计时行的「正在计时：X」照旧。

> 更名顺带撞出一处措辞重叠：统计页原有的「合并子集 / 展开子集」变成「合并子任务 / 展开子任务」，与列表的「展开」字面撞车。两者含义不同（一个是聚合维度，一个是折叠），且各在各自页面内，本次未另作区分。

#### 验证

- `npm test` **20/20 通过**（原 18，新增 2 条：默认展开且可收起 / 收起时点「+ 子任务」自动展开）
- `npm run build` 通过（tsc + vite）
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均为 **0**，v3 约束未被破坏
- 全仓库旧词零残留（`更新日志.md` 的历史条目按上文说明保留）
- 后端只动了报错文案与注释，**接口契约未变**
- **未做浏览器实测**：`TodoList` 需登录后才渲染，本地无后端，交互由测试覆盖

#### 部署结果：后端这一半**漏了将近两天**，2026-09-18 23:22 才补上

期二的前端部分当天随构建上了线，**后端部分一直没部署**。而更名里恰恰有三处是用户看得到的报错文案：

| 报错 | 触发场景 |
|---|---|
| `合集请分别给子集计时` | 给合集本身点开始计时 |
| `目标事项不是该合集的子集` | 迁移时长时选错了目标 |
| `子集下不能再加子集` | 给子任务再加子任务 |

于是这段时间线上是割裂的：界面写「子任务」，报错还写「子集」。

**怎么发现的**：本轮合并 PR #40 时打算走一遍部署，先确认「线上跑的到底是哪一版」，顺手比对了后端镜像的构建时间（09-17 23:42）与最后一次后端提交的时间（09-18 13:41）—— 镜像更早。**没有停在时间戳推断上**，把镜像里的 `app.jar` 取出来解出 class 验了一遍：

```
TodoService.class         旧文案「子集」= 2   新文案「子任务」= 0
TimeSessionService.class  旧文案「子集」= 2   新文案「子任务」= 0
```

**为什么会漏**：前端与后端的改动在同一次提交里，但部署动作是分开的（`docker compose up --build frontend` / `backend`）。前端有产物哈希可比对 —— 一构建文件名就变，部没部署一眼看得出；**后端没有这种信号，镜像名一直是 `todo-backend:latest`，容器也一直「Up」着**，不主动比对就没有任何提示。

**这条值得记住：改了后端就要单独跑一次 backend 的构建，别把「部署」当成一个整体动作。**

补部署（2026-09-18 23:22）：

```
cd backend && mvn -B clean package     # 本地构建，21.8s
→ 本地先验新 jar 里的 class：子集 0 / 子任务 2
→ --put target /root/todo/backend，双方 md5 一致（356a42d9…）
→ cd /root/todo && docker compose up -d --build --force-recreate backend
```

验证：`Started TodoApplication in 7.0 seconds`、Tomcat 8080；`GET /api/todos` 经 nginx 返回 401（未登录，链路通）、首页 200；**运行中容器里的 class 已是「子集 0 / 子任务 2」**（在这一层再验一遍，是因为「上传的 jar 是对的」与「跑起来的 jar 是那个」是两件事）。回滚点 `7f0f22df` 现已悬空，在保留的最近 3 个之内。

顺带比对了服务器与仓库的 `docker-compose.yml`、两个 `Dockerfile`、`nginx.conf`：去掉行尾差异后 md5 全一致 —— **陈旧的只有那个后端 jar**。

---

### 三、展开 / 收起加过渡动画（PR #18 · `a048d36`）

#### 动机

PR #17 给合集加了「展开 / 收起」开关，但收起、展开都是瞬间切换——子任务列表直接挂载 / 卸载，按钮也只是纯文字，看着生硬。

#### 改动

**按钮加箭头**：文字「展开 / 收起」旁加一个 chevron 图标，收起时指右、展开时指下，`transition-transform` 旋转过渡。

**子任务列表加高度动画**：新增 `Collapsible` 组件，用 JS 测量内容自然高度（`scrollHeight`）驱动 `height` 过渡——展开从 0 平滑展开，收起平滑缩回 0。

| 决策 | 取值 | 理由 |
|---|---|---|
| 动画实现 | JS 测 `scrollHeight` + `height` transition | 纯 CSS 的 `grid-template-rows: 0fr/1fr` 需 Chrome 107，T7 内核 97 不支持；`max-height` 收起有「前段不动」的延迟感 |
| 收起后的内容 | 动画结束**卸载**，不是 `visibility: hidden` | 隐藏但留在 DOM 的元素仍可被键盘 Tab 聚焦 |
| 动画结束判定 | `transitionend` + 300ms `setTimeout` 兜底 | jsdom 不触发 `transitionend`，测试靠兜底定时器 |
| 首次渲染 | 不做动画，直接落位 | 避免整页合集首屏一起「展开」闪动 |

#### 验证

- `npm test` **20/20 通过**（收起退场 300ms，两处「收起后子任务消失」断言改用 `waitFor`）
- `npm run build` 通过（tsc + vite），CSS 17.64 kB
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均为 **0**
- **未做浏览器实测**：动画手感（时长、缓动）需真机确认；本地无后端，测试只覆盖「收起后消失」的最终态，不覆盖动画过程

---

### 四、等级主题色随等级渐变，满级炫彩（PR #20）

#### 动机

等级徽章此前固定靛蓝，1 级和 499 级一个样，看不出「升了级」。游戏化等级该有品质色递进——低阶绿、高阶红，满级再来点炫的。

#### 改动

`level.ts` 新增 `levelColor` / `levelColorText`：1~499 级沿 **绿 → 蓝 → 紫 → 金 → 红** 六段锚点做 RGB 线性插值，每级微变；500 级满级走 CSS 炫彩（`index.css` 的 `.rainbow-bg` / `.rainbow-text`，彩虹渐变 + `background-position` 动画）。

`TodoList` 等级卡片三处跟随等级色：徽章底、称号文字（用加深版保证白底可读）、进度条填充。满级时三者全炫彩。

| 决策 | 取值 | 理由 |
|---|---|---|
| 渐变方式 | RGB 六段线性插值 | 比 HSL 色相插值直观可控，锚点直接对齐「品质色」 |
| 称号文字 | 底色 ×0.72 的加深版 | 绿/金直接当文字在白底对比度不足 |
| 满级判定 | `info.nextLevel === null` | 复用已有满级信号，不另设阈值 |
| 炫彩实现 | `background-clip:text` + `keyframes` | 纯 CSS，T7 内核（Chromium 97）支持，不受 Tailwind v3 约束影响 |

#### 验证

- `npm test` **23/23 通过**（新增 3：锚点色 / 相邻两级颜色不同 / 文字色为底色加深版）
- `npm run build` 通过，`.rainbow-bg` / `.rainbow-text` / `@keyframes rainbow-shift` 均进产物
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均为 **0**
- **未做浏览器实测**：渐变观感与炫彩动画需真机确认，本地无后端

---

### 五、满级炫彩动画首尾帧相连（PR #21）

#### 动机

满级（500 级）炫彩的彩虹流动动画循环到结尾会「跳」一下——`background-size:400%` 配 `background-position:0%→100%` 只位移了 3/4 个渐变周期，首尾帧图案对不上。

#### 改动

`index.css` 的 `.rainbow-bg` / `.rainbow-text` 把 `background-size` 由 `400%` 改为 `200%`，`@keyframes rainbow-shift` 终点由 `background-position:100%` 改为 `200%`。

关键在周期对齐：渐变首尾同色（`#ef4444`），图案周期 = `background-size`（一个 200% 背景宽）。百分比定位的位移是 `position × (容器宽 − 背景宽)`，终点 `200%` 对应位移恰好 2 倍容器宽 = 一个完整周期，于是 `0%` 帧与 `100%` 帧图案完全相同，循环无缝。原来 `400%` + `100%` 只位移 3 倍容器宽，落在周期的 3/4 处，跳变就出在这里。

| 决策 | 取值 | 理由 |
|---|---|---|
| 周期尺寸 | `200%`（原 `400%`） | 让「位移一个周期」对应的 position 是整数 `200%`，而不是 `400%` 下的 `133.33%` 魔数 |
| 终点方向 | `0% → 200%` | 与原来 `0% → 100%` 同向（背景向左流），只改位移量不改方向 |

#### 验证

- `npm test` **23/23 通过**（本次纯 CSS 改动，测试数不变）
- `npm run build` 通过，CSS 17.63 kB；产物中 `.rainbow-bg` / `.rainbow-text` / `@keyframes rainbow-shift` 均在
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均为 **0**
- 无缝性由周期计算保证（终点位移 = 一个完整渐变周期），**未做浏览器实测**：动画是否肉眼无缝需真机确认，本地无后端

---

### 六、满级炫彩改斜向分割线、向右上流动并加快（PR #22）

#### 动机

上一期把炫彩循环修成了无缝，但彩虹是水平条纹、向左流动、3 秒一个周期。要：分割线斜着、向右上流动、更快。

#### 改动

`index.css` 三处：

| 改动 | 原 | 新 | 作用 |
|---|---|---|---|
| 渐变方向 | `90deg` | `45deg` | 分割线（等色线）由竖线变 135° 斜线 |
| `background-size` | `200% 100%` | `200% 200%` | y 方向也留出一倍位移空间，斜向流动才动得了 |
| keyframes 终点 | `-200% 50%` | `-200% 200%` | x 取负（向右）+ y 取正（向上），向右上流动 |
| 动画时长 | `3s` | `2s` | 加快 |

无缝性不变：位移 `(2W, -2H)` = 平铺周期（`200%` 宽 / `200%` 高）的整数倍，与渐变方向无关，首尾帧图案仍相同。

#### 验证

- `npm test` **23/23 通过**（纯 CSS 改动，测试数不变）
- `npm run build` 通过，CSS 17.63 kB；产物含 `45deg` ×2、`background-size:200% 200%` ×2、`background-position:-200% 200%`、`rainbow-shift 2s` ×2
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均 **0**
- **未做浏览器实测**：斜向观感、速度、方向需真机目测，本地无后端

---

### 七、满级炫彩改回水平向右流动，速度再加快 50%（PR #23）

#### 动机

上一期把炫彩改成 45° 斜向分割线，实测斜向观感不对，回退水平；流动方向保持向右；速度再加快 50%。

#### 改动

`index.css`：

| 改动 | 原 | 新 | 作用 |
|---|---|---|---|
| 渐变方向 | `45deg` | `90deg` | 分割线回到竖线，水平条纹 |
| `background-size` | `200% 200%` | `200% 100%` | 去掉 y 方向位移，只水平流动 |
| keyframes 终点 | `-200% 200%` | `-200% 0%` | 向右，y 不动 |
| 动画时长 | `2s` | `1.333s` | 速度 +50% |

无缝性不变：x 位移 = 一个平铺周期（`200%` 宽），首尾帧图案相同。

#### 验证

- `npm test` **23/23 通过**（纯 CSS 改动，测试数不变）
- `npm run build` 通过，CSS 17.63 kB；产物含 `background-position:-200% 0%`、`background-size:200% 100%` ×2、`rainbow-shift 1.333s` ×2，`45deg` **零残留**
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均 **0**
- **未做浏览器实测**：方向与速度需真机目测，本地无后端

---

### 八、满级炫彩动画时长调到 1s（PR #24）

#### 动机

上一期把炫彩流动速度调到 1.333s 一个循环，仍偏慢，再调快到 1s。

#### 改动

`index.css` 两处 `animation` 时长 `1.333s` → `1s`。

#### 验证

- `npm test` **23/23 通过**（纯 CSS 改动，测试数不变）
- `npm run build` 通过，CSS 17.63 kB；产物 `rainbow-shift 1s` ×2，`1.333s` 零残留
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均 **0**
- **未做浏览器实测**：速度观感需真机目测，本地无后端

---

### 九、等级越高字号越大、称号字体逐级变帅（PR #25）

#### 动机

等级卡片此前所有等级一个字号、一套系统默认字体，1 级和 500 级除了颜色没有任何「分量」差别。要：**字号随等级放大**，**字体逐级变得更有性格**，新手档保持默认字体不动。

#### 关键约束：手机上取不到中文书法字体

Windows 上有楷体、华文行楷、隶书等，但 **Android 系统只带一种中文字体（思源黑体）**，网页通过 `font-family` 调不到书法字体。而本项目当初锁 Tailwind v3 正是因为手机端（百度 App / T7 内核）是真实目标，所以「换字体」这件事在手机上**只有自托管字体文件一条路**。

选择**子集化自托管**：只嵌入实际用到的字符，两个字合计约 15 KB。

#### 改动

**字体（`src/assets/fonts/`）**

| 文件 | 用途 | 大小 |
|---|---|---|
| `wenkai-subset.woff2` | 霞鹜文楷（楷体，清秀） | 8.80 kB |
| `smiley-subset.woff2` | 得意黑（斜体，冲击力） | 6.13 kB |
| `OFL-LXGWWenKai.txt` / `OFL-SmileySans.txt` | 两份 OFL 许可证 | — |

**子集字符从 `level.ts` 现抽**，不手写：脚本用正则读出 `RANKS` 里全部 `title` 值，加上 `Lv.` 与 0-9，共 **35 个字符**。这样以后改称号名，重新跑一次子集即可，绝不会出现「称号改了、字体里没这个字」的脱节。

> 霞鹜文楷的 OFL 带一条 ADDITIONAL PERMISSION，**明确允许子集化用于 web 字体分发**，正是本用法。

**字号与字体分档（`level.ts` 新增 `levelFont`）**

```ts
badgeSize: 12 + t * 5,   // 12 → 17 px
titleSize: 14 + t * 12,  // 14 → 26 px
family: lv > 400 ? 得意黑 : lv > 50 ? 霞鹜文楷 : null,  // null = 页面默认
```

| 档位 | 等级 | 字体 |
|---|---|---|
| 新手 | 1~50 | **页面默认**（按要求不动） |
| 学徒 ~ 首席 | 51~400 | 霞鹜文楷 |
| 泰斗 ~ 登峰造极 | 401~500 | 得意黑 |

字号是**连续**放大的（每级都变），字体是**分档**跳变的。系统字体名（`KaiTi` / `STKaiti`）留在 font-family 后备链里，字体没加载完也不会变成系统默认衬线。

**渲染（`TodoList.tsx`）**：徽章与称号去掉写死的 `text-xs` / `text-sm`，改为内联 `fontSize`；字体族仅在 `family` 非 null 时注入，新手档不写 `fontFamily`。

#### 验证

- `npm test` **26/26 通过**（新增 3：新手档 family 为 null / 三档字体边界 50·51·400·401 / 字号单调递增且端点值为 12·14 与 17·26）
- `npm run build` 通过，两个 woff2 被 Vite 输出并加内容哈希，CSS 由 17.63 → 17.92 kB
- **子集字符覆盖校验**：构建产物中两个字均覆盖 **35/35**，无缺字（用 fontTools 读 cmap 逐字比对）
- 构建前后 woff2 的 **md5 一致**，Vite 未改动字体二进制
- Tailwind 内核自检：`@layer` / `@property` / `oklch(` / `color-mix(` 均 **0**
- 联网体积增量：字体 14.93 kB + CSS 0.29 kB ≈ **15.2 kB**
- **未做浏览器实测**：字体观感、档位切换是否突兀、大字号下换行是否挤，需真机确认

---

### 十、满级换云峰飞云体、泰斗换马善政毛笔楷书（PR #26、#27）

#### 动机

上一期给称号分了档：51~400 用霞鹜文楷、401~500 用得意黑。实际看下来两处都不对：

1. **得意黑不适合满级。** 它本质是一款**窄体**，字形天生瘦长、笔画细。用在「登峰造极」这种要撑场面的最高档上，恰恰是最没分量的一个。反馈原话：「瘦长瘦的，而且字体也不帅，不霸气」。
2. **泰斗与登峰造极该分开。** 401~499 是「次高」、500 是「最高」，共用一套字体等于把最后一档的独特性抹平了。

方向：换成**毛笔书法类**的粗壮字体。

#### 关键约束：这个字体品类的授权与体积互相打架

毛笔书法字体是本项目的**特殊品类**，有三条限制同时成立：

| 限制 | 说明 |
|---|---|
| **必须自托管** | Android 只带思源黑体，`font-family` 调不到任何书法字体（上一期已确认） |
| **天然很大** | 笔锋需要大量轮廓控制点。同为 6763 字，几何标题体约 1 MB，毛笔体 **3 MB 起步**，江湖体可到 25 MB |
| **多数禁止修改** | 「不允许修改，任何人不得修改或制作衍生版本」—— 而**子集化正是制作衍生版本** |

第三条是死结。上一期那两个字之所以只有 15 KB，是因为 OFL 明确允许子集化；而免费商用的书法字体绝大多数是**作者声明**授权，白纸黑字写着不许改。

调研过并排除的（连同原因，避免以后重复调研）：

| 字体 | 排除原因 |
|---|---|
| 温州鹿城瓯韵体 / 云峰游戏标题简体 / 站酷仓耳渔阳体 | 授权**禁止嵌入**（网页 / APP），正是本用法 |
| 优设书华体 | **付费**（个人商用 ¥69 / 企业 ¥199） |
| 李旭科书法 / 禹卫书法行书 / 叶根友系列 | 授权不明，商用通常需单独授权 |
| 平方塞北体 / 刘昂乐天体 / 赖江湖各体 / 飞波正点体 / 阿里妈妈东方大楷等 | 允许嵌入，但**明文禁止修改**，无法子集化；整包引入则最小 3.08 MB（塞北体），最大 25.94 MB（赖江湖飞扬体） |

**决策**：接受「**整包引入、不做任何修改**」。这样不触碰「禁止修改」条款，只需单独处理体积。

#### 改动

**泰斗（401~499）→ 马善政毛笔楷书**（OFL 1.1，可自由子集化）

| 文件 | 用途 | 大小 |
|---|---|---|
| `mashan-subset.woff2` | 马善政毛笔楷书子集 | 12.22 kB |
| `OFL-MaShanZheng.txt` | OFL 1.1 许可证 | — |

**满级（500）→ 云峰飞云体**（整包，字节不改）

| 文件 | 用途 | 大小 |
|---|---|---|
| `yunfeng-feiyun.ttf` | 云峰飞云体，**未做任何修改** | 7,928,508 B（7.56 MB） |
| `授权说明-云峰飞云体.txt` | 上游授权说明原文 | — |

入库时校验 md5 与上游原文件一致（`A95A9519…`）——「一个字节都没动」这件事要**可验证**，不能只是声明。

**删除得意黑**：`smiley-subset.woff2` 与 `OFL-SmileySans.txt` 因本次改动成为孤儿，一并移除。

**分档（`level.ts` 的 `levelFont`）**

```ts
family: lv >= MAX_LEVEL ? 云峰飞云体 : lv > 400 ? 马善政 : lv > 50 ? 霞鹜文楷 : null,
```

| 档位 | 等级 | 字体 |
|---|---|---|
| 新手 | 1~50 | 页面默认 |
| 学徒 ~ 首席 | 51~400 | 霞鹜文楷（8.80 kB） |
| 泰斗 | 401~499 | 马善政毛笔楷书（12.22 kB） |
| 登峰造极 | 500 | 云峰飞云体（7.56 MB） |

**7.56 MB 怎么消化**

直接发给每个访客不可接受。两条措施：

1. **按需下载。** `@font-face` 本身不预取字体，浏览器只在**真有元素用到该字体族**时才下载。云峰飞云体只出现在 500 级的称号与徽章上，故 1~499 级用户**一次都不会触发**这笔流量。
2. **nginx gzip**（`nginx.conf`）。只压传输、不动文件：磁盘上的 `.ttf` 与上游原文件字节一致。**线上实测 7,928,508 B → 5,001,907 B**（压缩率 63%）。`gzip_types` 刻意不含 `woff2`——那已是压缩格式，列进去白费 CPU。

两项合起来：受影响范围从「所有访客」缩到「满级用户」，单次体积从 7.56 MB 降到 5.00 MB；配合 `/assets/` 既有的 `immutable` 缓存头，每人只下一次。

**让 gzip 真正生效，踩了两个坑（都不报错，只是静默失效）**

1. **`gzip_types` 按响应头的 `Content-Type` 匹配，而 nginx 自带的 `mime.types` 里没有 `.ttf` 条目。** 不显式指定的话 `.ttf` 落到 `default_type`（`application/octet-stream`），于是 `gzip_types` 里写的 `font/ttf` 永远匹配不上，压缩**静默不生效** —— 部署后第一次 `curl` 复验，加不加 `Accept-Encoding: gzip` 都是 7,928,508 B。修法是给 `/assets/*.ttf` 单独一条正则 location 用 `default_type font/ttf` 纠正（正则 location 优先于前缀 location，故缓存头要重复一次）。顺带把字体类型也标对了，对浏览器更正确。
2. **部署脚本 `ssh-todo.sh --put <目录> <远程目录>` 是把「目录本身」解到远程目录下**，不是把目录内容摊进去。上传 `dist` 恰好符合直觉（落到 `/root/todo/dist`），于是上传 `nginx.conf` 时我照抄了同一写法，结果落到 `/root/todo/nginxstage/nginx.conf`，而 `/root/todo/nginx.conf` **始终没被替换** —— 容器重建了、配置却还是旧的，看起来一切正常。第二次复验才发现。以后传单文件要显式 `mv` 就位，并**用 md5 核对远端文件**，别只看「命令没报错」。

#### 验证

- `npm test` **26/26 通过**。改了 1 条：三档 → 四档，并改为断言各档 font-family 的**首选项**而非包含关系 —— 四个档的后备链里都含 `LevelKai`，沿用 `toContain("LevelTaidou")` 式的写法会让 401 级的断言即使取到霞鹜文楷也照样通过
- `npm run build` 通过；三个字体被 Vite 输出并加内容哈希，CSS 里的引用路径与产出文件名逐一核对一致
- **字节校验**：`yunfeng-feiyun.ttf` md5 与上游原文件一致（7,928,508 B）
- **子集覆盖校验**：马善政子集覆盖 **35/35** 字符（fontTools 读 cmap 逐字比对）
- **Tailwind v3 自检**：`@layer` / `@property` / `oklch(` / `color-mix(` 均 **0**
- 得意黑残留检查：产物 CSS 中 `LevelSmiley` / `smiley` 各 **0** 处
- **线上 gzip 实测（部署后）**：字体 `Content-Type: font/ttf` + `Content-Encoding: gzip`，7,928,508 B → **5,001,907 B**；`.css` / `.js` 同样带 `Content-Encoding: gzip`；`.woff2` 未被误压；`/api/` 反代 `401`（未带 token，符合预期）
- **未做浏览器实测**：按需下载是否真如预期不触发、7.56 MB 加载期间 `font-display: swap` 的回退观感，均需真机确认

#### 遗留的授权边界（如实记录）

云峰飞云体采用「作者声明」授权，随包的 `字体授权说明.txt` 只写了「个人和企业都可免费商用」，并指向猫啃网的介绍页；**「网页嵌入」这一项是猫啃网授权表列为"可以"，属于第三方整理、页面自注"仅供参考"，并非作者的明文条款**。

也就是说：这与 OFL（授权本身就明确覆盖嵌入与修改）**性质不同**，属于**授权留白**。本决策的前提是「自用项目、风险可控」，不是「授权已明确覆盖本用法」。日后若本项目商业化或对外大规模分发，这一条需要重新评估。

---

### 十一、目录整理：前端独立成 `frontend/`、文档归入 `docs/`（PR #31）

**动机**：`backend/` 自成一体，前端却有 10 个文件散在仓库根目录（`index.html`、`vite.config.ts`、`tsconfig.json`、`tailwind.config.js`、`postcss.config.js`、`package.json`、`package-lock.json`、`src/`、`Dockerfile`、`nginx.conf`），两边不对称。另有 `rank.txt`（等级系统的需求草稿，文件名看不出是什么内容）与一个 2.2MB 的哈希名 png 躺在根目录。

**整理后**

```
frontend/   index.html vite.config.ts tsconfig.json tailwind.config.js
            postcss.config.js package.json package-lock.json src/ Dockerfile nginx.conf
backend/    未动
docs/       更新日志.md、需求草稿-等级与称号系统.md（原 rank.txt，改名以见名知意）
根          docker-compose.yml  .gitignore  README.md  start-todolist.bat
```

全用 `git mv` 搬运，保留文件历史。删除 `c878c2e7993b5f3b616e2e9f34959aaf.png`（未跟踪垃圾）。

**改动到的引用点**（这是「保证路径能运行」的全部家当）

| 位置 | 改动 | 为什么 |
|---|---|---|
| `docker-compose.yml` | frontend 的 `build.context`：`.` → `./frontend` | 构建上下文变了，`Dockerfile` 里的 `COPY dist/`、`COPY nginx.conf` 才有东西可拷 |
| `start-todolist.bat` | 加 `cd /d "%~dp0frontend"` | 用 `%~dp0`（脚本自身所在目录）定位，**从桌面快捷方式启动也能工作**，不受快捷方式「起始位置」影响。文件名与路径刻意保持不变，指向它的快捷方式不受影响 |
| `README.md` | 命令加 `cd frontend`；`更新日志.md` → `docs/更新日志.md`；目录结构一节重写 | 路径变了 |
| 本项目 `CLAUDE.md` | 命令、目录结构、部署路径全部跟着改 | 同上 |

**没有改、也确认不需要改的**（相对路径，移动后天然有效）：`vite.config.ts` 的 `setupFiles: "./src/test/setup.ts"`、`tsconfig.json` 的 `include: ["src"]`、`tailwind.config.js` 的 `content: ["./index.html", "./src/**/*.{ts,tsx}"]`、`src/index.css` 里的 `url("./assets/fonts/...")`、`.gitignore`（`node_modules` 与 `dist` 是无锚点模式，任意层级都匹配）、`backend/Dockerfile`。

#### 验证：产物逐字节未变，所以**不需要重新部署**

整理前后各 `npm run build` 一次，比对 `dist` 全部 7 个文件的 md5 与相对路径：

```
36263df78df82285f3f656478a8df97f assets/wenkai-subset-B1TG98Hi.woff2
3936ee127afec086eb8bbfd97ffa9018 index.html
4015813ded6e1b792ce4df5061035e0d assets/Stats-DGa5pA6O.js
442ee4f696804a3dacebf0087d7ef221 assets/mashan-subset-DobLZWs1.woff2
a95a951901fa4e39eb3b91aa6923bd24 assets/yunfeng-feiyun-CmceRSBf.ttf
b371892b41ed6ed7ad54d57d0228c8f5 assets/index-D6PGEnpg.js
d6fba569be5d35bc98891b8338c8990b assets/index-DZoplmLO.css
```

**完全一致** —— 源码路径不进 bundle，连内容哈希文件名都没变。线上跑的就是这一份，**没有任何重新部署的必要**。`npm test` 在 `frontend/` 下同样 26/26 通过。

#### 顺带发现：`docker-compose.yml` 从来没有被部署过

核对服务器配置时发现 `/root/todo/docker-compose.yml` 与 `Dockerfile` 的日期停在 **9-15**，本地是 9-17，md5 也对不上。**进一步核实是虚惊**：差异纯粹来自行尾（本地 CRLF、服务器 LF），归一化后内容逐字节一致。

但它暴露了机制上的真问题：**部署流程只上传 `dist/`、`nginx.conf`，从不上传 `docker-compose.yml` 与 `Dockerfile`**。这次改了 `build.context`，如果照旧不上传，就会「仓库改了、服务器没改」，下次构建直接挂。此后把 `docker-compose.yml` 纳入部署流程。

#### 服务器布局已同步迁移

改 `build.context` 就意味着服务器那份也得动，否则下次部署必挂。已执行（合并 PR #31 之后）：

```
/root/todo/ 下：mkdir frontend/
             → mv dist Dockerfile nginx.conf frontend/
             → 换上新的 docker-compose.yml（md5 bad854be...，与仓库 LF 版逐字节一致）
             → 删掉备份的旧 compose（已核对与整理前的 git 版本内容一致，无信息量）
             → docker compose up -d --build --force-recreate --no-deps frontend
```

**重建后的镜像 ID 与迁移前完全相同**（`sha256:153b8a20…`）—— 层全部命中缓存，因为 `dist/` 与 `nginx.conf` 一个字节都没变。这比单纯比 md5 更能说明「整理没有改变任何构建产物」：连 docker 层摘要都一样。所以这次重建**没有任何新内容上线**，只是让服务器目录结构与仓库对齐。

迁移后复验：首页 200、`/api/todos` 401、字体 `Content-Type: font/ttf`（gzip 修复仍在）、`Accept-Encoding: gzip` 时 7,928,508 → **5,001,907 B**，与本期改 gzip 时记录的实测值一致。

**验证的边界**：只重建了 frontend 服务（`--no-deps`），backend 与 mysql 未动；回滚路径未实测（也没必要 —— 镜像 ID 都没变，等于没换版）。

---

### 十二、称号留艺术字体，徽章「Lv.N」改回默认字体（PR #33）

**动机**（用户提出）：徽章上的等级数字不该用艺术字体 —— 数字和 `Lv.` 本来就该一眼认得出，毛笔体的辨识度反而是负的。称号（新手 / 学徒 / 泰斗 / 登峰造极）保留艺术字体。

#### 改动

`levelFont()` 返回的 `family` 从此只管称号，故改名 `titleFamily`（免得日后有人又把它接到徽章上）。徽章 `style` 里去掉 `fontFamily`，**字号仍随等级放大**，所以徽章只是「字体变默认」，大小不变。

| 位置 | 改动 |
|---|---|
| `frontend/src/level.ts` | `LevelFont.family` → `titleFamily`；注释写明只作用于称号 |
| `frontend/src/TodoList.tsx` | 徽章 `style` 去掉 `fontFamily`；称号那处改用 `titleFamily` |
| `frontend/src/index.css` | 注释同步 |
| `frontend/src/level.test.ts` | 跟着改名（2 处断言） |
| `frontend/src/TodoList.test.tsx` | **新增**一条组件测试 |

#### 验证

新增「徽章『Lv.N』走页面默认字体，艺术字体只给称号」：预置一条攒满 500 小时的会话把事项顶到满级（满级正是云峰飞云体那一档），断言**徽章 `style.fontFamily` 为空、称号含 `LevelPeak`**。

为此给 api mock 加了 `__seed(todos, sessions)` —— 等级由累计时长决定，要凑到满级只能预置数据，界面点击点不出来。另一个坑：mock 下没有登录态就不会 `loadData()`，测试得先往 localStorage 塞 `todo-token`（第一版就栽在这里，界面渲染出「暂无任务」）。

**测试确实有效**：把徽章的 `fontFamily` 加回去跑一遍 → 该条变红（`expected '"LevelPeak", …' to be ''`），撤掉 → **27/27 绿**。没有只靠「新写的测试也是绿的」就下结论。

#### 子集里 13 个字符就此闲置（明确决定不动）

两款 woff2 是本期之前按「所有称号 + `Lv.` + 数字」共 35 字嵌的，徽章改默认字体后 `Lv.` 与 0~9 这 13 个字再没人用。**没有重做子集** —— 只占几 KB，重做要再动一次字体文件、再过一遍授权，不划算。已在 `index.css` 注释里写明，免得日后看到 cmap 多出字符以为是 bug。

满级那档不受影响：云峰飞云体 7.56 MB 本来就是整包引入、只在 500 级称号上引用，徽章改动不改变它的加载时机。

#### 部署：这次产物真的变了

与上一期「不必部署」不同，入口 chunk 哈希变了，老老实实走了一遍完整流程。

```
清空 /root/todo/frontend/dist → 上传 frontend/dist
→ 比对清单（7 个文件 md5 全同） → docker compose up -d --build --force-recreate --no-deps frontend
```

`nginx.conf` 与 `docker-compose.yml` **这轮没传** —— 先核对过内容一致才跳过的（见下面那条 CRLF 的坑），不是忘了。

- 镜像：`153b8a20…` → `7c3053fa…`，旧的那层成了悬空镜像（回滚点）。各版独有的仍只有 `dist` 那一个 COPY 层
- 复验：首页 200、入口 JS 200、`/api/todos` 401、字体 `font/ttf` + gzip **5,001,907 B**、旧哈希文件 `index-D6PGEnpg.js` 已 **404**（清空生效）

**怎么证明「徽章改默认字体」真的上线了**（没有浏览器时的办法）：`fontFamily` 是 DOM style 的属性名，压缩不会改名，可以直接在产物里数赋值点。

| 产物 | `fontFamily` 赋值点 | 上下文 |
|---|---|---|
| 旧（镜像 `153b8a20`） | **2** | `` `${O.badgeSize}px`,…O.family?{fontFamily:…} `` 与 `` `${O.titleSize}px`,… `` 各一处 |
| 新 | **1** | 只剩 `` `${O.titleSize}px`,…O.titleFamily?{fontFamily:…} `` |

徽章那一行整个消失了。这比「测试是绿的」更进一步 —— 绿的是源码，这里是**线上正在跑的字节**。

#### 又一次 CRLF 假警报：`nginx.conf`

准备上传时发现服务器 `nginx.conf` 的 md5（`714ae387`）和仓库（`4cd9e723`）对不上，像配置漂移。拉下来逐行 diff：**57 行全部报告「不同」，内容却一模一样** —— 服务器那份是 CRLF，我比对前只给本地做了 `tr -d '\r'`。

这与 9-15 那次 `docker-compose.yml` 是**同一个坑，隔三天又踩一次**。教训：**跨机器比 md5 前，两边都要归一化行尾**，否则任何文本文件都会常年「漂移」。`core.autocrlf=true` 让工作区是 CRLF、仓库里是 LF，服务器上那份又取决于当初怎么传的 —— 三方各说各话。

（顺带：`docker-compose.yml` 这次 md5 完全一致，因为上次上传用的是 LF 版。所以「服务器上是 CRLF」并非必然，取决于上传时本地工作区的状态。）

#### 另一个可复用的观察：`Stats-*.js` 换了名字，代码没换

`Stats-DGa5pA6O.js` → `Stats-CaUJ6zOE.js`，大小一模一样（10,056 B）。把线上那份旧的拉下来 diff 新产物，**唯一差异是它 import 入口 chunk 的那行路径**（`./index-D6PGEnpg.js` → `./index-mEGqzn8y.js`），自身代码逐字节相同 —— Rollup 的 chunk 哈希会吸收依赖 chunk 的哈希。

**「文件名变了」不等于「这段代码变了」，别拿文件名当变更范围的证据。**

---

### 十三、称号字体一档一款，学徒到泰斗各换一款（PR #35 · `f7fdaa6`）

**动机**（用户提出）：上一期之后，51~400 级仍然共用霞鹜文楷一款字体 —— 八档里只有三档看得出区别。用户要求**每档各用一款不同字体**，越往上越帅，但不要毛笔味太重的那种。

#### 选型：从 140 款里挑，逐档点名

候选池取自**中文网字计划**（`@chinese-fonts/*`，84 个包 / 140 款），按字形气质分四类出了 104 款备选图（楷 / 宋明 / 黑圆方 / 手写艺术）供用户逐档点名。最终：

| 档 | 等级 | 字体 | 引入方式 | 大小 |
|---|---|---|---|---|
| 学徒 | 51–100 | MapleMono-CN Regular（等宽黑） | 原样分片 ×2 | 47.2 KB |
| 研习者 | 101–150 | 霞鹜文楷 Regular | 原样分片 ×2 | 40.5 KB |
| 实践者 | 151–200 | 思源黑体 粗 | **自子集化** | 1.7 KB |
| 能手 | 201–250 | 江西拙楷 | 原样分片 ×2 | 39.6 KB |
| 资深 | 251–300 | 程荣光刻楷 | 原样分片 ×2 | 47.0 KB |
| 顾问 | 301–350 | 瑞美加张清平硬笔行书 | 原样分片 ×2 | 55.7 KB |
| 首席 | 351–400 | 鸿雷行书简体 | 原样分片 ×2 | 58.1 KB |
| 泰斗 | 401–499 | 志莽行书（钟齐） | 原样分片 ×2 | 73.8 KB |
| 登峰造极 | 500 | 云峰飞云体 | 整包 | 7.56 MB（未改） |

新手（1–50）仍走页面默认字体；徽章「Lv.N」仍一律走默认字体（上一期的结论不变）。

**怎么在不下整包的前提下看 140 款字体**：中文网字计划把每款切成 130~400 片 woff2，jsdelivr 能单片取。逐片读 cmap 建「字符 → 分片」映射，渲染时逐字挑覆盖它的那一片 —— PIL 没有 `unicode-range` 概念，只能自己查表。同一母体的各分片度量一致，逐字换字体不会错位。15 个目标字通常只落在 9~15 片里，140 款全下也只 **42.1 MB**，而不是几十 GB。

选型阶段另有两个坑值得记：

- **「这字没渲染出来」多半是字面率差异，不是缺字。** 同一字号下得意黑撑满字身框、千图笔锋手写体只有一半高，拼在一张图上就像后者没画。按 `getbbox()` 的墨迹高度线性归一到同一视觉大小后即正常 —— 一度按「渲染 bug」排查了三轮，方向是错的。
- **cmap 里有码位 ≠ 有真字形。** 用私用区码位（U+E000 起）渲一张「空模板」，把每个目标字的位图与它逐像素比对，才查出「平方萌萌哒」「qiji-fallback」这类字体里一堆字其实指向 `.notdef`。据此剔掉 4 款占位字字体。

#### 引入方式由授权决定，不是由体积决定

104 款候选里，能按 OFL 自由子集化的只是一部分。最终点名的九款分两类处理：

**其一，四款非 OFL 字体不走子集化。** 江西拙楷 / 程荣光刻楷 / 鸿雷行书简体 / 瑞美加张清平硬笔行书都是「作者声明免费商用」，其中程荣光刻楷与鸿雷行书简体的公开条款**明文写着「不可修改字体及制作衍生版本」**，另两款（江西拙楷、瑞美加）只说了可商用、**没有**授予修改权 —— 未授予即不可推定。而**子集化就是修改**（删字形、改 cmap）。

于是改为：**原样搬运中文网字计划已经切好的 woff2 分片，一个字节都不改**，只取覆盖本档那两三个字的那一两片，按 `unicode-range` 引用。分片是该计划发布给浏览器用的形态，原样引用不属于我方对字体的修改。这比「整包引入」的再分发量还小一到两个数量级。

**其二，实践者（思源黑体粗）自己子集化。** 它是 OFL，且中文网字计划里没有这一款（该计划无思源黑体），故取 Noto Sans SC Bold 整包用 fontTools 子集到「实践者」三字，1.7 KB。

代价是**同目录下并存两种机制**。没有强行统一成一种：非 OFL 那四款若也整包引入，就是 3~25 MB × 4；若也子集化，就越过了授权红线。机制的分裂是授权本身的分裂，写在了 `index.css` 顶部注释里。

> 一度考虑把三款 OFL 字体（MapleMono / 霞鹜文楷 / 志莽行书）也改成自子集化，能省约 200 KB。查了 MapleMono-CN 的发行包是 **134 MB**（含全 CJK），为几十 KB 去下它不划算，遂放弃 —— **最终统一为「中文网字计划有的就原样搬分片，没有的才自子集化」**。

#### 改动

| 位置 | 改动 |
|---|---|
| `frontend/src/level.ts` | `Rank` 新增 `font` 字段，八档字体族**并入 `RANKS` 表**；`levelFont()` 由写死档位判断改为查 `rankOf()`；删掉 `FONT_KAI` / `FONT_TAIDOU` / `FONT_PEAK` 三条常量 |
| `frontend/src/index.css` | 3 个 `@font-face` → **16 个**（16 = 14 个分片/子集 + 满级整包 + 无），顶部注释重写 |
| `frontend/src/assets/fonts/` | 新增 14 个 woff2、3 份 OFL、4 份授权说明 |
| `frontend/src/level.test.ts` | 「四档递进」改为「八档逐档断言」+ 新增「八档互不相同」 |
| `frontend/src/TodoList.test.tsx` | **未改** —— 原断言是 `toContain("LevelPeak")`，满级那档本轮没动，仍然成立 |

字体族放进 `RANKS` 表而非另起一张映射表，是为了**让「档位 ↔ 称号 ↔ 字体」三件事只有一个定义处**：上一版把档位划分写在 `RANKS`、字体选择写在 `levelFont()` 的嵌套三元里，两处一旦不同步就要靠测试兜。

`@font-face` 段是**脚本从构建产物的 `plan.json` 生成后贴入**的，不是手抄 —— 每档的 `unicode-range` 动辄上百个码位，手抄必错。

#### 验证

- `npm test` **27/27 通过**。改了 1 条：原「四档递进」断言里 `first(400) === '"LevelKai"'` 这类写法在八档下会失去意义，改为遍历八档、**同时断言每档的上下界落在同一款字体上**，并加一条「八档字体族互不相同」—— 防止漏改某一档时留下重复项却仍全绿
- `npm run build` 通过（tsc + vite），14 个 woff2 均被 Vite 输出并加内容哈希
- **cmap 覆盖校验**：fontTools 逐份读产物 cmap，比对本档称号的字是否都在 —— 八档全部命中
- **`unicode-range` 反查**：解析 `index.css` 里全部 `@font-face` 的 `unicode-range`，反查每档称号的每个字是否落在**本档**声明的范围内 —— 八档全部覆盖。这条能同时挡住「分片取错」和「范围抄错」两类错误，而只查 cmap 挡不住后者
- **Tailwind v3 自检**：`@layer` / `@property` / `oklch(` / `color-mix(` 均 **0**
- 旧字体族残留扫描：产物中 `LevelKai` / `mashan` / `wenkai-subset` 各 **0** 处
- **未做浏览器实测**：按需下载的时机（高等级用户的称号是否真如预期只拉自己那两片）与 `font-display: swap` 的回退观感，均需真机确认

#### 遗留的授权边界（如实记录）

与上一期云峰飞云体那条同源，且这次涉及四款：

- 四款都是「作者声明免费商用」，**其中两款明文禁止修改、另两款未表态允许修改**
- 本项目按字面最保守处理：**一个字节都不改**，只原样引用上游已发布的分片。**但这仍属授权留白** —— 分片是中文网字计划切的，其再分发权来自该计划对字体的理解，不是作者的明文条款
- 这与 OFL（授权本身明确覆盖嵌入、修改与再分发）**性质不同**。前提是「自用项目、风险可控」，不是「授权已明确覆盖本用法」
- 四份说明随字体落在 `frontend/src/assets/fonts/授权说明-*.txt`，各写明「本项目怎么用的、为什么这么用、哪些场景需要另行确认」

**未做的事**：没有联系任何一位作者确认。若日后本项目商业化、对外大规模分发，或要把这些字体用于商标 / 出版物 / 软件内嵌，这一条需要重新评估。

#### 删除

`mashan-subset.woff2`（马善政，原 401~499 档）、`wenkai-subset.woff2`（上一版的霞鹜文楷子集，本轮按档重做）、`OFL-MaShanZheng.txt` 因本次改动成为孤儿，一并移除。

#### 部署

合并走了 merge commit：`a606887`，**两个父提交**（`683cbfd` + `d774e08`），原提交 SHA 都在 `main` 里 —— `git merge-base --is-ancestor HEAD origin/main` 通过，没有分叉。

配置**没传**：`nginx.conf` 与 `docker-compose.yml` 两边 md5 一致（各为 `4cd9e723` / `bad854be`），**且这次是归一化行尾后比的** —— 第十二期那个 CRLF 假警报的教训已经固化成步骤了。这两个值本轮相对上一期未变，说明它们确实没被改过，不是「忘了传」。

```
清空 /root/todo/frontend/dist（原 6 个文件 / 7.8 MB）
→ 上传 frontend/dist → 清单 18 vs 18 一致 → md5 18/18 一致
→ docker compose up -d --build --force-recreate --no-deps frontend
```

- 镜像：`7c3053fa` → **`e61b5974`**；旧的那层成了悬空镜像（回滚点）
- 复验：首页 200、入口 JS 200（167,886 B）、`index-DUZByJUN.css` 200、`/api/todos` **401**、旧哈希文件 `index-D6PGEnpg.js` **404**（清空生效）
- **线上 CSS 反查**：16 个 `@font-face`，15 个字体文件引用；`LevelKai` / `mashan` / `wenkai-subset` 在线上 CSS 中各 **0** 处
- **入口 JS 的 `fontFamily` 赋值点仍是 1** —— 与第十二期同法，确认徽章那处没有回来
- gzip：`.woff2` 未被压（34,028 B 进 34,028 B 出），`.ttf` 7,928,508 → **5,001,907 B**，`.css` 28,320 → 9,245 B
- 外网实测：`http://47.106.200.121:8310/assets/LevelTaidou-1-DA07szsD.woff2` → 200 + `font/woff2`

#### 一个不查就会误判的地方：产物里少了 `LevelShijianzhe.woff2`

`dist/assets` 只有 18 个文件，**没有 `LevelShijianzhe.woff2`** —— 一度以为是漏了文件。

实际是 Vite 的 `build.assetsInlineLimit` 默认 **4096 B**，而这份子集只有 **1,780 B**，被**内联成 data URI** 写进了 CSS。核对方式：把线上 CSS 里的 base64 解出来，与源文件比 md5 —— **1,780 B 对 1,780 B，一致**。

所以「产物里没有这个文件」不等于「这个文件没进产物」。实践者这一档反而省了一次请求。副作用是 CSS 从 17.23 kB 涨到 28.32 kB（含内联的 1.78 KB 与八档的 `unicode-range`），gzip 后 9.20 kB。

#### 镜像清理

frontend 悬空镜像 5 个 → 保留最近 3 个（`7c3053fa` / `153b8a20` / `45ece768`），逐个 `docker rmi` 删掉 `6c4288dc` 与 `0c187792`，均一次成功（未被占用）。backend 另有 2 个悬空镜像，未超 3，未动 —— **两个服务各算一份**。

#### 未做浏览器实测

按需下载的时机（八档的字体是否真如预期只拉自己那一两片）与 `font-display: swap` 的回退观感，仍**没有在真机上确认过**。这是本轮唯一没覆盖到的环节。

---

### 十四、称号字号拉大到 10→36，徽章拉大到 10→18（PR #37）

#### 动机

用户提出：「称号从 10 到 36，徽章从 10 到 18」。

上一期的区间是称号 `14 → 26`、徽章 `12 → 17`，跨度太窄 —— 称号整整 500 级只拉开 12px，低等级那一段（新手到研习者，1~150 级）几乎看不出大小差别；满级 26px 也压不住「登峰造极」四个字的分量。这一期把**起点压低、终点抬高**，两端都往极限推。

#### 改动

两行常数，`frontend/src/level.ts` 的 `levelFont()`：

```diff
-    badgeSize: 12 + t * 5,  // 12 → 17
-    titleSize: 14 + t * 12, // 14 → 26
+    badgeSize: 10 + t * 8,  // 10 → 18
+    titleSize: 10 + t * 26, // 10 → 36
```

`frontend/src/level.test.ts` 同步改断言（原写死 12/14 与 17/26，现写死 10/10 与 18/36，单调性那半段未动）。`frontend/src/TodoList.tsx` **未改** —— 字号本来就是从 `levelFont()` 读的。无新增依赖、无新文件。

#### 两端都推到了极限，但中间有一段反而变小了

这是这次改动**最该被记住的一点**：区间不是整体平移，是**绕一个交叉点旋转** —— 低等级更高等级各自向两端拉伸，中间必然有交叉。新旧两式相等的位置：

| | 交叉点 | 含义 |
|---|---|---|
| 称号 | 约 **144 级**（`14+12t = 10+26t` → `t=2/7`） | 144 级以下**比原来小**，以上比原来大 |
| 徽章 | 约 **334 级**（`12+5t = 10+8t` → `t=2/3`） | 334 级以下比原来小，以上比原来大 |

各档起始等级的实际取值：

| 档 | 等级 | 徽章 旧→新 | 称号 旧→新 |
|---|---|---|---|
| 新手 | 1 | 12.00 → **10.00** | 14.00 → **10.00** |
| 学徒 | 51 | 12.50 → 10.80 | 15.20 → 12.61 |
| 研习者 | 101 | 13.00 → 11.60 | 16.40 → 15.21 |
| 实践者 | 151 | 13.50 → 12.40 | 17.61 → 17.82 |
| 能手 | 201 | 14.00 → 13.21 | 18.81 → 20.42 |
| 资深 | 251 | 14.51 → 14.01 | 20.01 → 23.03 |
| 顾问 | 301 | 15.01 → 14.81 | 21.21 → 25.63 |
| 首席 | 351 | 15.51 → 15.61 | 22.42 → 28.24 |
| 泰斗 | 401 | 16.01 → 16.41 | 23.62 → 30.84 |
| 登峰造极 | 500 | 17.00 → **18.00** | 26.00 → **36.00** |

注意「实践者」这一档称号 17.61 → 17.82 只多了 0.2px，几乎无感；而新手掉到 **10px**，比正文（`text-sm` 14px）还小。若日后觉得新手那档太小、或者中间段的「反而变小」不可接受，改的是**两个端点值**，不是加偏移。

#### 验证

- `npx vitest run` **27/27 通过**（改 1 条字号断言，其余未动）
- `npm run build` 通过（tsc + vite），入口 chunk 变为 `index-BSDOzh-a.js`（167.20 kB）
- **1:1 实尺预览图**：这次要判断的是「10px 会不会太小、36px 会不会太大」，所以预览图**按真实像素画、不放大**（上一期放大 3 倍是为了看字体长相，这次放大就失去意义了）。版式照抄 `TodoList.tsx`（徽章 `px-2 py-0.5` + `rounded-md`、标题与徽章 `gap-2`、右侧 `text-xs`、下面 `h-2.5` 进度条），字体直接取自要进仓库的 woff2
- **未做浏览器实测**：10px 的中文在手机上的实际可读性只有真机能判断，尤其百度 App 的 T7 内核

#### 一处已知落差：线上会比预览图更粗

称号那行是 `font-semibold`（600），而 `index.css` 里 16 个 `@font-face` **全部声明 `font-weight: 400`** —— 两者对不上，浏览器会**合成假粗体**（faux bold）。PIL 不合成，所以预览图比线上「干净」一些。

字号拉到 36px 后这个落差比上一期明显（笔画被横向加粗，行书体本来就密的笔画会更糊）。**本轮没改**，因为它属于另一个问题（字重声明 vs 类名），且要动的是 `@font-face` 或那行 className，属于视觉改动 —— 留给用户决定。一行 `font-synthesis: none` 即可关掉。

---

### 十五、每一级都有自己的色域炫动，速度随等级递增；满级提速 1.5 倍（PR #38）

#### 动机

用户提出：「500 级速度再快 1.5 倍，1 级是完全不动，每一级都要颜色略微向右炫动，但保持在自己的色域附近，等级愈高，炫动速度越快，但 499 应该是新 500 级速度的 0.8 倍，1 级是完全不动，逐级增加到 499」。

此前**只有满级**在动（整条彩虹），1~499 级全是静止纯色 —— 500 级和 499 级之间没有任何过渡感。

#### 速度：一条直线，两端按用户给的比例钉死

速度以「每秒跑完几个渐变周期」计，三个锚点直接来自用户：

| 锚点 | 速度 | 周期 | 依据 |
|---|---|---|---|
| 1 级 | 0 | 不动 | 「1 级是完全不动」 |
| 499 级 | 1.2 周期/秒 | **0.83s** | 新 500 级速度 × 0.8 |
| 500 级 | 1.5 周期/秒 | **0.67s** | 原来 1 周期/秒（`.rainbow-*` 的 `1s`）× 1.5 |

2~498 级在 0 与 1.2 之间**按等级线性插值**，即 `速度(lv) = (lv-1)/498 × 1.2`：

| 等级 | 周期 | 等级 | 周期 |
|---|---|---|---|
| 1 | 不动 | 250 | 1.67s |
| 2 | 415.00s | 300 | 1.39s |
| 50 | 8.47s | 400 | 1.04s |
| 100 | 4.19s | 499 | 0.83s |
| 167 | 2.50s | 500 | 0.67s（整条彩虹） |

**低等级实际等于不动。** 2 级的周期是 415 秒，10 级 46 秒 —— 这是「速度线性」的必然结果（速度 ∝ lv-1，周期就是它的倒数），不是 bug。真正看得出流动是 50 级往后。若想让低等级也动起来，改的是**插值方式**（比如对速度取对数），不是调端点。

**499 → 500 有一处速度跳变**（0.83s → 0.67s）：这是用户点名的比例（499 = 新 500 的 0.8 倍），故意留的台阶，不是插值断了。500 级同时换回**整条彩虹**——它不参与「本档色域」这条规则。

#### 幅度：色相 ±8°、明度 ±14%

「略微……保持在自己的色域附近」落成两个常数（`level.ts`）：

```ts
const SHIMMER_HUE_DEG = 8;    // 色相左右各偏这么多度
const SHIMMER_LIGHT_PCT = 14; // 明度上下各偏这么多个百分点
```

每级按自己的主题色生成 `linear-gradient(90deg, lo, mid, hi, mid, lo)` —— **首尾同色**，配合 `background-size: 200%` 与既有的 `rainbow-shift`（向右滚一个周期）形成无缝循环。称号那一条用的是**本色加深版**（`× 0.72`，同 `levelColorText`），否则在白底上对比度不够。

这两个数**是我选的默认值**，动图里同时给了另外两档备选（±4°/±7%「更轻」、±14°/±24%「更明显」），用户看完再定。

#### 改动

| 位置 | 改动 |
|---|---|
| `frontend/src/level.ts` | 新增 `levelShimmer()`、`toHsl()`、`hsl()`、`shimmerGradient()` 与 4 个常数；`levelRgb()` 被复用（仍然是私有） |
| `frontend/src/index.css` | `.rainbow-bg` / `.rainbow-text` 时长 `1s → 0.667s`；新增 `.shimmer-bg` / `.shimmer-text` |
| `frontend/src/TodoList.tsx` | 徽章与称号各多一条分支：满级走 `rainbow-*`，2~499 走 `shimmer-*` + 行内 `backgroundImage` / `animationDuration`，1 级保持原来的纯色 |
| `frontend/src/level.test.ts` | 新增 3 条（见下） |

**颜色只能写在行内**：每级色域不同，`background-image` 没法预先写进样式表。所以样式表里的 `.shimmer-*` 只管**形状**（`background-size`、`background-clip`、keyframes），颜色与时长走行内 —— `1s` 只是兜底默认值，实际每次都被行内 `animationDuration` 覆盖。

`hsl()` 用**逗号写法**（`hsl(143.0, 70.4%, 45.1%)`），不用空格分隔或 `oklch()` —— T7 内核（Chromium 97）只认老写法，这与「Tailwind 锁 v3」是同一条约束。

#### 验证

- `npx vitest run` **30/30 通过**（原 27 + 新增 3）。新增的三条：① 1 级与满级都返回 `null`；② 499 级时长 = `1/1.2`，且 2~499 逐级单调变快；③ 渐变首尾同色、且 5 个停靠点里中间与两端不同（不是一整块纯色）
- `npm run build` 通过，入口 chunk `index-CgPkmkvM.js`（168.17 kB，CSS 28.50 kB）
- **产物断言**：CSS 里 `.shimmer-bg` / `.shimmer-text` 都在、满级是 `rainbow-shift .667s`、`background-clip:text` 共 4 处（两个类的 `-webkit-` 与标准各一）；入口 JS 里 `animationDuration` 与 `backgroundImage` 各 **2 个赋值点**（徽章 + 称号，多一个少一个都是错的）；`@layer` / `@property` / `oklch(` / `color-mix(` 仍全部为 **0**
- **预览不是重写一遍算法**：把 `level.ts` 经 esbuild 导成 ESM 跑出真实的渐变字符串，Python 只按浏览器规则（sRGB 线性插值 + `background-size:200%` 位移）把它画出来，**并先断言两边逐字相同**才出图。踩到一个坑：Python 的 `round()` 是银行家舍入而 JS 的 `Math.round()` 是四舍五入，250 级的 r 分量正好落在 `206.5` 上，差 1 就对不上
- **未做浏览器实测**，也**未在真机跑过**

#### 两个如实记录的风险

**其一，性能。** 此前只有满级（罕见）在动；现在**列表里每一行都有两个动画元素**（徽章 + 称号），且动的是 `background-position`——不是合成层属性，每帧都会重绘；称号还叠了 `background-clip: text`，重绘更贵。低端手机上会不会掉帧，**只有真机能判断**。若卡，退路是把动画只留给称号、或只给前 N 行。

**其二，`color: transparent` 的失效模式。** `.shimmer-text` 靠 `background-clip: text` + 透明字色显示文字；一旦背景图没生效而透明字色生效，**文字会直接消失**。这两个属性在同一个渲染里设置，风险很低，且满级自第十期起就是这个结构，但这属于「知道它坏起来是什么样」的一类问题，记在这里。

---

### 十六、炫动速度重调：1 级从「完全不动」改为 20s 一圈，499 级改为 1.145s（PR #39）

本条目覆盖**同一次交付里的连续两轮**调整。两轮都落在 PR #39 里，第一轮那个中间状态**从未上线**，所以合成一条记。

#### 动机

第一轮，用户提出：「登峰造极变成速度原来的 0.8 倍，499 变成现在的 0.7 倍，对应的所有等级都修改」。

第二轮紧接着，改的还是同一处：「1 级改成 20 秒，499 改成 1.145 秒，中间的等级都对应更改」。

**只动速度，幅度仍是上一期那两个默认值**（色相 ±8°、明度 ±14%），用户还没定夺。

#### 第一轮的「原来的」歧义，以及为什么那么解

「登峰造极变成速度**原来的** 0.8 倍」有两种读法：

| 读法 | 结果 | 是否自洽 |
|---|---|---|
| 第十期以来的 1.0 周期/秒 × 0.8 | 0.8 周期/秒（1.25s） | ✗ 比 499 级的 0.84 **还慢**，与「等级愈高，炫动速度越快」直接矛盾 |
| 上期跑着的 **1.5** × 0.8 | **1.2 周期/秒**（0.833s） | ✓ 仍是全线最快，499 那句的「现在的」也正好指 1.2 |

取后者：「原来的」= 改动前正在跑的 1.5，与下一句「499 变成**现在的** 0.7 倍」是同一个参照点。第二个依据是硬约束 —— 满级慢于 499 级会让这条曲线自相矛盾。

这一轮算出 500 级 1.2 周期/秒（0.833s）、499 级 0.84（1.190s）。**500 级那个值留到了最后，499 级的在第二轮被替换掉了。**

#### 第二轮：端点改成「直接给秒数」，1 级不再静止

用户不再给倍数，而是**直接给周期**：1 级 20 秒、499 级 1.145 秒。

这一轮有个语义变化：**1 级此前是「完全不动」**（`levelShimmer(1)` 返回 `null`，走纯色分支），现在它也要动 —— 这一点单独问过用户并得到确认。于是 `null` 的含义收窄成「只有满级」：

| 等级 | 十五期 | 十六期第一轮 | **最终** |
|---|---|---|---|
| 1 | 不动（`null`） | 不动（`null`） | **20.000s**（0.05 圈/秒） |
| 499 | 0.83s | 1.19s | **1.145s**（≈0.8734 圈/秒） |
| 500 | 0.67s | 0.83s | 0.83s（未再变） |

#### 插值方式：速度线性，不是周期线性

用户给的是两个**周期**（秒），很容易顺手续成「周期线性递减」。但两者只在端点相同，中段能差到 5 倍：

| 等级 | 按速度线性（采用） | 按周期线性 |
|---|---|---|
| 100 | **4.68s** | 16.25s |
| 250 | **2.17s** | 10.57s |
| 400 | **1.41s** | 4.89s |

**这组对比摆给用户看过，用户选了速度线性** —— 它延续「等级愈高，炫动速度越快」的匀加速语义，也是既有算法的延续，改动最小。公式仍是 `v(lv) = v1 + (lv-1)/498 × (v499 - v1)`，时长取其倒数。

最终各档：

| 等级 | 周期 | 速度（圈/秒） | 等级 | 周期 | 速度 |
|---|---|---|---|---|---|
| 1 | 20.000s | 0.0500 | 300 | 1.837s | 0.5443 |
| 2 | 19.360s | 0.0517 | 400 | 1.409s | 0.7097 |
| 10 | 15.413s | 0.0649 | 416 | 1.358s | 0.7361 |
| 50 | 7.633s | 0.1310 | 499 | 1.145s | 0.8734 |
| 100 | 4.680s | 0.2137 | 500 | 0.833s | 1.2（整条彩虹） |
| 250 | 2.166s | 0.4617 | | | |

**499 → 500 仍有台阶**（1.145s → 0.833s，速度跳升 1.375 倍），比第一轮的 1.43 倍略缓。这是「499 不参与彩虹」的必然结果，不是插值断了。

低等级仍然很慢（10 级 15.4s 一圈），但**不再是「完全不动」** —— 这是本轮唯一的变化点，也是用户要的。

#### 连带清理：两处死分支

`levelShimmer(1)` 不再返回 `null` 之后，TodoList.tsx 里「非满级又没有 shimmer → 用纯色」的兜底分支成了**永不可达的死代码**。按「清掉自己造成的孤儿」一并处理：

- 删掉局部变量 `titleColor` 与 `level.ts` 的 `levelColorText()` —— 后者的唯一调用者就是这条死分支
- JSX 里原先的 `isMaxLevel ? {} : shimmer ? {…} : { color: titleColor }` 收窄成 `shimmer ? {…} : {}`：**`shimmer` 为 `null` 与 `isMaxLevel` 互为反面**，不必判两次
- `badgeColor` / `levelColor()` **保留** —— 等级进度条的填充色仍在用
- `tsconfig` 开着 `noUnusedLocals`，不清理会直接构建失败

#### 改动

| 位置 | 改动 |
|---|---|
| `frontend/src/level.ts` | 端点常数改为 `LV1_CYCLES_PER_SEC = 1/20` 与 `LV499_CYCLES_PER_SEC = 1/1.145`；`levelShimmer()` 只有满级返回 `null`；删 `levelColorText()` |
| `frontend/src/index.css` | 注释说明满级那 1.2 只写在 CSS 里、`level.ts` 不再管它；`.shimmer-*` 的注释改掉「1 级保持纯色不动」 |
| `frontend/src/TodoList.tsx` | 删死分支与 `titleColor`；两处三元由三层收窄成一层 |
| `frontend/src/level.test.ts` | 「1 级完全不动」改成「只有满级不参与本档炫动」；速度测试拆成「两端点就是用户给的秒数」+「周期逐级变短」两条；删掉 `levelColorText` 那条 |

#### 验证

- `npx vitest run` **30/30 通过**（删 1 条、加 1 条，总数不变）
- `npm run build` 通过，入口 chunk `index-Bo4ke-g2.js`（168.00 kB，比上轮的 168.17 kB 略小 —— 死代码删干净了）
- **产物断言**：CSS 里两处 `rainbow-shift` 是 `0.833s`、`.shimmer-*` 的兜底 `1s` 不变；JS 里 `backgroundImage` 与 `animationDuration` 各 **2 个赋值点**，而 `backgroundColor` 已从行内样式里**彻底消失（0 个）** —— 这正是死分支被删掉的证据；`@layer` / `@property` / `oklch(` / `color-mix(` 仍全为 **0**
- CSS 产物的哈希与上轮**完全相同**（`index-CEDRQXXM.css`，28.50 kB）：本轮 CSS 只动了注释，注释不进产物。**哈希没变 ≠ 源码没改过**，与上轮那条「体积相同 ≠ 没变化」是同一类陷阱的两面
- **预览不是重写一遍算法**：从 `level.ts` 经 esbuild 导出真实渐变与时长，Python 复算后**先断言逐字相同**才出图（反查的等级里这一轮加上了 1 级）
- **动图的循环约束变了，值得记**：上一轮各档周期恰好都是 50/7 的有理倍数，挑得到真公倍数；本轮跨度 17 倍（20s ↔ 0.83s）且都除不尽，**公倍数不存在**。于是改成「对齐」—— 每行动效周期取循环长度的整数分之一（`L/round(L/T)`）。取 L = 20.00s：1 级正好 1 圈、满级正好 24 圈，**用户点名的两端零误差**，中段最大偏差约 7%（100 级）；图上标注写的仍是真实周期。这个偏差是「每行都真无缝」的代价，换别的 L 只会更差
- **动图体积从 8.2MB 压到 1.8MB**：`disposal=2` 让 PIL 每帧存整幅、帧间差分失效；改成 `disposal=1` 后只存变化区域。帧间实际只有徽章与称号那一小块在动，所以收益极大
- **未做浏览器实测**，也**未在真机跑过**

#### 仍未定夺

- **炫动幅度**：仍是上期我选的默认值（色相 ±8°、明度 ±14%）。动图第一段照旧摆着两档备选（±4°/±7%「更轻」、±14°/±24%「更明显」），等用户定
  - **2026-09-19 收口**：第十九期试过「幅度随等级递增」、第二十期又逐版收窄与归零，用户逐版看下来全部否掉，最后**回到这两个默认值**。两档备选始终没被选中 —— 结论就是这套 ±8°/±14°
- **假加粗**：称号 span 带 `font-semibold`（600），而 16 个 `@font-face` 声明全是 `font-weight: 400`，浏览器只能合成加粗。预览工具（PIL）不做合成，所以**动图看起来比线上干净**。上期就提过，本轮未改

---

### 十七、给 admin 灌入 10 条称号示例数据（生产数据操作，**无代码改动**）

#### 动机

用户要求「给 admin 用户添加所有称号对应的示例」。称号的字体、颜色、字号、炫动全都按等级算，要看全 10 档的实际效果，只能让账号里真的存在 10 个不同等级的待办。

#### 为什么绕过 API 直接写库

等级依据的是**单个待办**的累计时长 —— `elapsedMs(todoId)` = 该待办自己的已完成会话 + 其子任务的会话（`store.tsx`）。后端只有「开始 / 暂停」这类接口，时长是 `Date.now()` 的差值，**造不出任意历史时长**。所以直接写 `todos` 与 `time_sessions` 两张表。

#### 数据

每档取**中点**：同一档内字号与颜色也随等级连续变（称号 10→36px），中点最能代表这一档的样子。登峰造极只有 500 一级，没有中点可挑。

| 称号 | 目标等级 | 累计时长 | 称号 | 目标等级 | 累计时长 |
|---|---|---|---|---|---|
| 新手 | 25 | 6.8 h | 资深 | 275 | 180.9 h |
| 学徒 | 75 | 26.6 h | 顾问 | 325 | 238.3 h |
| 研习者 | 125 | 53.9 h | 首席 | 375 | 303.2 h |
| 实践者 | 175 | 88.7 h | 泰斗 | 450 | 414.6 h |
| 能手 | 225 | 131.1 h | 登峰造极 | 500 | 498.3 h |

时长取自 `level.ts` 的 `cumulativeMinutes(level)`，**喂回 `getLevelInfo()` 逐条断言「等级与称号都等于目标值」之后**才生成 SQL（10/10 通过）。合计 1942.4 小时。

注意满级那行是 **498.3 h 而不是 500 h** —— 满级门槛是「完成前 499 级」的累计，500 小时是「完成 500 级」的总和，而 500 级之后没有下一级。

待办文字统一为 `称号示例 · <称号>`，`completed = 0`、无子任务、`createdAt` 递增（列表顺序即等级升序）。

#### 时间戳刻意排在 2020 年

`start`/`end` 落在 2020-01-01 ~ 2020-03-01。统计页只有 day/week/month 三种分桶，放这里**一条都不会落进去**，不会把真实统计图冲掉。也便于日后识别与清理 —— `created_at between 1577836800000 and 1577836809999` 就是当时核对用的筛选条件（用时间戳而非 `text like '%称号示例%'`，是为了避开在远程 shell 的单引号里嵌中文）。

#### 踩到的坑：中文被双重编码（**截至写下这段时，那 20 行仍是坏的**）

灌完用户就反馈「示例名字是乱码」。查下来问题在**导入路径**，不在数据本身：

- MySQL 官方镜像不设置 locale（容器里 `LC_CTYPE=POSIX`），`mysql` 客户端的 `default-character-set=auto` 据此猜成 **latin1**
- 于是 UTF-8 的 SQL 文件被按 latin1 解释，每个字节各自升格成一个字符：`E7 A7 B0`（「称」）→ `C3A7 C2A7 C2B0`

**反直觉的地方是：我用 `docker exec mysql` 查回来看到的中文是正常的。** 显示路径上又做了一次方向相反的转换（utf8mb4 → latin1 输出 → 终端按 UTF-8 解释），两次错误恰好抵消。**只有 `hex()` 是硬证据**：库里存的是 `C3A7C2A7C2B0…`，正确值应是 `E7A7B0…`。前端走 JDBC（UTF-8 正确链路），所以用户实际看到的是 `ç§°å·ç¤ºä¾‹ Â· æ–°æ‰‹`。

同一张表里 user_id=4 那条待办的 hex 是 `E7A4BA…`（正常的「示例」），说明**应用写入完全没问题**，坏的只有我手工灌的这 10 条 `todos` + 10 条 `time_sessions`。

修法：按下面两行 `delete` 清掉，再用带 `--default-character-set=utf8mb4` 的命令重灌同一份 `seed.sql`（文件本身是正确的 UTF-8，错的只是导入时的解释方式）。

**第一次发现时没有当场修** —— 用户先要求查根因，改生产数据需要单独授权，所以停在那里等。这个边界本身是对的：查根因和改生产数据是两件事，前者不需要许可，后者需要。

用户随后授权，修复已执行并验证：

- 修复前 `hex(text)` 是 `C3A7C2A7C2B0…`，修复后是 `E7A7B0…`（「称」的正确 UTF-8）
- 10 条 `todos` + 10 条 `time_sessions` 数量不变，用 utf8mb4 客户端读出来是「称号示例 · 新手」等正确中文
- **重灌前核对了种子文件的 md5**（本地与服务器同为 `7639d8c2…`），排除「文件在上传途中坏掉」—— 少了这一步，万一上传坏了，修完还是乱码，会误判成「方法不对」再白查一轮

防护写在「环境约束备忘」第 7 条。

#### 怎么清理

```sql
delete from time_sessions where todo_id in
  (select id from todos where user_id = 5 and created_at between 1577836800000 and 1577836809999);
delete from todos where user_id = 5 and created_at between 1577836800000 and 1577836809999;
```

**先删会话。** `time_sessions` 对待办**没有外键约束**（实体注释里写明「删除待办后保留历史，用于统计」），所以删待办不会连带删会话 —— 顺序反了会留下一批孤儿会话，继续算进统计。

---

### 十八、两个分发产物：PWA（降级版）+ Electron 桌面外壳

用户要求「PWA 和 exe 各做一个，放到 GitHub Release 上」。两者都不是功能改动，是**让别人用得上**的两条路。

#### PWA 只能做成「降级版」，原因在 HTTPS

先说清楚代价，因为这不是取舍问题而是硬限制：

- **`navigator.serviceWorker` 在非安全上下文里根本不存在。** 站点是 `http://47.106.200.121:8310/`，公网 IP + 明文 HTTP 不构成安全上下文（只有 HTTPS、localhost、file:// 才是）
- Chrome 的「可安装」判定本身就要求 HTTPS。所以 Android Chrome 上「添加到主屏幕」**只会建一个带地址栏的普通书签**，不是独立 App

要变成真 PWA，得先有域名解析过来、开放 443、配 nginx TLS + 证书（Let's Encrypt 不给纯 IP 签证书）—— 那是动生产服务器的事。用户知情后选了**降级版：不动服务器**。

于是本轮做的是：`manifest.json` + 4 个图标 + `index.html` 的 head 标签。**没写 service worker** —— 写了也注册不上，那是死代码，等 HTTPS 到位再加。

iOS 的 `apple-mobile-web-app-capable` 走的是另一套机制（不依赖 HTTPS），所以 **iOS 上「添加到主屏幕」能真的独立窗口打开，Android 不能** —— 这个不对称是这次唯一实际拿到的收益。**未实测**：手边没有 iOS 设备。

#### 文件名是 `manifest.json`，不是 `.webmanifest`

惯例后缀是 `.webmanifest`，但它在这套部署里会被**当成 `application/octet-stream` 发出去，Chrome 直接拒收**。原因是 nginx 的 `mime.types` 里没有这个后缀 —— 实测线上 nginx/1.31.5，整个文件里只有一行 json：`application/json  json;`，`grep -i manifest` **零命中**。

这是「环境约束备忘」第 8 条（gzip 按 `Content-Type` 匹配）的同类坑：**产物对了、配置对了，栽在 MIME 上**。用 `.json` 则命中 nginx 自带的映射，且 Chrome 接受 `application/json`，**不需要改 nginx 配置** —— 代价为零，所以没有去加 `types` 块。

#### 四个图标各有各的规矩，不是一张图缩四遍

| 文件 | 画法 | 为什么 |
|---|---|---|
| `icon-192` / `icon-512` | 圆角方块，四角透明 | `purpose: any`，当普通图标用 |
| `icon-maskable-512` | **满幅**方块，图形缩进安全区 | Android 会自己套遮罩（圆形、方圆形…），图形超出安全区会被切掉 |
| `apple-touch-icon`（180） | **满幅**方块，图形比例稍大 | iOS 自己切圆角 —— 带透明角的会被它切出**黑边** |

配色取 App 自己的主色 `blue-500`（`#3b82f6`，登录按钮和主要按钮用的就是它），没有另起一套。对勾画好后在 24px 下仍认得出，这是选它的实际标准。

#### Electron 外壳：独立成 `desktop/`，不塞进 `frontend/`

`main.js` 里**没有任何应用逻辑** —— 窗口 `loadURL` 的就是线上站点，代码和数据都还在服务器上。它的全部价值是「双击即用」：不必记 IP 和端口，有自己的图标和窗口。

**`APP_URL` 是写死的**，所以换域名或上 HTTPS 都必须**重新打包发版**。这条要记住：站点地址和 exe 是绑在一起的两个产物。

独立成 `desktop/` 而不是加进 `frontend/`，是因为 `frontend/` 是**部署用的 Docker 构建上下文**，不该混进 electron 这类只在本地用的依赖。

两个形态都发（用户选的）：

| 产物 | 体积 | 特点 |
|---|---|---|
| `TodoList-1.0.0-portable.exe` | 70.9 MB | 双击即用，不写注册表；每次启动自解压到临时目录，首次慢一两秒 |
| `TodoList-1.0.0-setup.exe` | 71.1 MB | NSIS 安装向导，可改安装路径，建开始菜单与桌面快捷方式 |

**没有代码签名证书，所以 exe 未签名。** 别人下载后 Windows SmartScreen 会拦一道，得点「更多信息 → 仍要运行」。这不是 bug，是没买证书的必然结果。

#### 踩到的两个坑（都在构建环节，不在代码里）

**① `npm install` 直接崩，且报错读不出原因。** electron 的二进制不在 npm 上，是 postinstall 里从 GitHub Releases 拉的，国内直连被掐，进程以 `3221226505` 退出。解法是在 `desktop/.npmrc` 里落 npmmirror 镜像 —— 这两个键会变成 `npm_config_electron_mirror` / `npm_config_electron_builder_binaries_mirror`，正好是 electron 的 `install.js` 和 electron-builder 的 `binDownload.js` 要读的环境变量名。**验证方式：清空 electron 二进制缓存、不设任何环境变量、纯净 `npm install` 通过**（否则本地装过一次就永远测不出镜像到底有没有生效）。

**② electron-builder 解压 `winCodeSign` 失败。** 那个包里含 macOS 的 `.dylib` **符号链接**，而 Windows 上创建符号链接需要管理员权限或开发者模式，于是解压报「客户端没有所需的特权」并重试 4 次。绕过办法是手工解压、用 `-x!darwin` 跳过含符号链接的目录，再把它放到 electron-builder 期望的缓存位置 `winCodeSign-2.6.0`（数字名的那几个目录是解压失败后残留的临时目录，不是它要找的名字）。**`rcedit-x64.exe` 就在这个包里** —— 它正是给 exe 写图标和版本信息的工具，所以这个缓存不能省。

#### 验证

- `npm run build` 通过，`dist/` 根下出现 4 个图标 + `manifest.json`；**JS/CSS 哈希与本轮之前完全一致**（`index-Bo4ke-g2.js` / `index-CEDRQXXM.css`）—— 这次只加了 head 标签和静态文件，没碰任何组件
- `npm test` **30/30 通过**；Tailwind v3 自检 `@layer` / `@property` / `oklch(` / `color-mix(` 仍全为 **0**
- Electron 外壳先做**隐藏窗口冒烟测试**（不弹窗）：加载线上站点后标题为 `TodoList`、React 已挂载
- 打包后验：版本信息确实写进 exe（`ProductName=TodoList`、`FileVersion=1.0.0`、`CompanyName=YinnnH2`、描述文案正确）、图标已嵌入、`app.asar` 里就是 `main.js` + `package.json` 两个文件
- **实际启动一次**打包好的 exe：5 秒后仍在运行（4 个 Electron 进程），关闭后无残留进程
- **未做**：没在别人的机器上试过；没实测 SmartScreen 的实际拦截表现；iOS 的独立窗口未实测（无设备）

#### 图标定稿：候选 ①「蓝底白勾」

三个候选（预览图见桌面 `应用图标-三选一.png`）里用户选了 ①：App 主色 `blue-500` + 白勾，最直白，24px 下也认得出。另两个是 ②「深底蓝勾」（slate-900 打底）与 ③「绿→蓝渐变」（呼应等级色域）。

**图标定在打包之前，是因为它会被烤进 exe** —— 改图就得重新打包、重新发版，所以这一步不能拖到发布之后。同一批图同时供 PWA（4 个尺寸）和 exe（`desktop/build/icon.ico`，7 档）使用，两边同源。

#### 部署与发布结果

PR #42 走 merge commit 合入，main = `60c999d`。

**前端部署**（PWA 那部分不部署就不生效，所以这次必须部）：

- `npm run build` → `dist/` 24 个文件。**JS/CSS 哈希与本轮改动之前完全一致**（`index-Bo4ke-g2.js` / `index-CEDRQXXM.css`）—— 反过来印证了这次确实只动了 head 标签和静态文件，没碰任何组件
- 先**清空**远程 `frontend/dist`（上传是解包覆盖、只增不删），再上传；比对 **24/24 md5 逐条一致**
- `docker compose up -d --build --force-recreate frontend`；回滚点是上一版镜像 `effc5790ff76`，新版 `todo-app:latest` = `803cd4c08e42`

线上验证（curl，**不是浏览器**）：

| 请求 | 结果 |
|---|---|
| `/` | 200，head 里 7 个新标签齐全 |
| `/manifest.json` | **200 `application/json`** |
| `/icon-192.png`、`/icon-512.png`、`/icon-maskable-512.png`、`/apple-touch-icon.png` | 全 200 `image/png` |
| `/api/todos`（未登录） | 401 |

`/manifest.json` 拿到 `application/json`，正是选 `.json` 而非 `.webmanifest` 要的结果 —— 这是本轮唯一一处「换个文件名就绕开 MIME 坑」的验证点。

**镜像保留**：frontend 悬空 4 → **3**（删最老的 `e61b597492ed`，逐个 `rmi`、不加 `-f`）；backend 恰好 3，**一个都没删** —— 不是漏做，是额度正好用满。redis1/2/3 未受影响。

**Release**：tag `v1.0.0` → `60c999d`，https://github.com/22145555734/todo-list/releases/tag/v1.0.0

| asset | 体积 | SHA256 |
|---|---|---|
| `TodoList-1.0.0-portable.exe` | 70.9 MB | `293b2a073599026ac68857134a3701f5c559e9a2c7e39368ef25f520801b82b3` |
| `TodoList-1.0.0-setup.exe` | 71.1 MB | `370bdaa9736d0c13e291892a138c23a71a07aed8c0d2e86b7a1750f5e0874fdf` |

发 Release 时踩到两个坑，都跟代码无关：

**① `target_commitish` 不接受短 SHA。** 传 `60c999d` 直接 422，错误体是 `tag_name is not a valid tag` + `target_commitish` invalid —— **报错指向 tag 名，实际是 commitish 没解析出来**，按字面去查 tag 名会白查。换成完整 40 位即成功。下次直接给 `git rev-parse` 的结果。

**② 70 MB 的 asset 用 Python `urllib` 传，在「写出」阶段超时**（`TimeoutError: The write operation timed out`）。这与站点无关，是往 `uploads.github.com` 上行大文件的固有速率问题。换 `curl --data-binary @file`，配 `--max-time 3000 --speed-limit 4096 --speed-time 180`（低速即断、而不是死等）后两个文件都传成。

**重跑上传要注意**：Release **本体在第一次就建好了**，只是 asset 没传上。所以补传要单做「上传 asset」这一步，把建 Release 的代码再跑一遍会撞 422（tag 已存在）。

**验证边界（明确没做的）**：没在真机上点过「添加到主屏幕」—— iOS 无设备，Android 即使点了也只会得到带地址栏的书签（非安全上下文），**这个结论来自规范与 Chrome 的判定条件，是推断不是实测**。也没在别人的机器上跑过这两个 exe，SmartScreen 的实际拦截表现同样未实测。

---

## 2026-09-17

本次共五期改动，第 2、3 期是同一问题的两层根因，按发现顺序记录。

---

### 一、前端渲染与打包优化（PR #4 · `e3dda85`）

依据 `react-performance` skill 的客户端规则（项目为 React 18 + Vite 纯 SPA，无 SSR/RSC，故仅适用客户端侧规则）。

| 改动 | 对应规则 | 效果 |
|---|---|---|
| `elapsedMs` 预计算为 `Map` | `js-` Map 查找 | 每个 todo 由「每次渲染扫全量 sessions」O(n²) 降为 O(1) |
| 提取 `TodoItem` 为 `memo` 组件 + 局部 `useNow(running)` | `rerender-` 提取 memo 组件 | 计时运行时**只有正在计时的那个 item 每秒重渲染**，其余项与整个列表不再跟着 tick |
| 统计页 `React.lazy` 懒加载 | `bundle-` 动态导入 | Stats 拆为独立 chunk（9.26 kB），首屏 JS 变小 |
| `FILTERS` / `TABS` 静态数组提到模块级 | `rerender-` hoist 静态配置 | 避免每次渲染重建数组 |

**验证**：构建通过；测试 13/13 通过。

---

### 二、修复移动端样式全部失效：Tailwind v4 → v3（PR #5 · `03aac73`）

#### 现象

手机端**只显示最朴素的 UI，没有任何排版和样式**；PC 端正常。

#### 排查与根因

先确认**不是** CSS 请求失败。服务器 nginx 访问日志显示请求完整成功：

```
"GET /assets/index-CIgt4UZ3.css" 200 20619
"GET /assets/index-D33A21lE.js"  200 160289
```

CSS 以 **HTTP 200 / 20619 字节**完整送达，是**解析阶段被拒**而非请求被拦。

真正原因：**Tailwind v4 用原生级联层输出样式**。产物 CSS 结构：

```
CSS 总长 20619 字节
@layer properties  偏移     65
@layer theme       偏移   1422
@layer base        偏移   3625
@layer utilities   偏移   7346  ← 一直到文件结尾
.mx-auto / .bg-white / .rounded-xl / .p-6 … 全部在 @layer utilities 内
```

`@layer utilities` 一段 **13273 字节，占整个样式表的 64%**。CSS 规范中浏览器遇到不认识的 at-rule 会**把整个块连同内部规则一起丢弃**，于是页面退化为无样式 HTML。

**触发条件是用户设备的内核版本。** 从日志取到实际 UA：

```
Linux; Android 16; PJZ110 … Chrome/97.0.4692.98 … T7/15.54 … baiduboxapp/15.55.0.10
```

一加13 / Android 16，但百度 App 用的是**自家 T7 内核 = Chromium 97**，而：

| 特性 | 需要版本 | Chromium 97 |
|---|---|---|
| `@layer` | Chrome **99** | ❌ 差 2 个版本 |
| `oklch()` / `color-mix()` | Chrome **111** | ❌ 差 14 个版本 |
| ESM（JS 能否执行） | Chrome 61 | ✅ 满足 |

这解释了「**页面有内容但完全没样式**」这个组合：JS 跑得了（所以有内容），CSS 被丢弃（所以没样式）。

**问题不在设备**——同一台手机的系统 WebView 是 Chromium 130+，用系统浏览器打开完全正常。是 App 自带内核滞后。

#### 改动

Tailwind **v4.3.3 → v3.4.17**（v3 输出纯 CSS，官方支持 Chrome 45+ / Safari 12+）：

- 卸载 `@tailwindcss/vite` + `tailwindcss@4`，安装 `tailwindcss@3.4` + `postcss` + `autoprefixer`
- 新增 `tailwind.config.js`（content: `index.html` + `src/**/*.{ts,tsx}`）与 `postcss.config.js`
- `vite.config.ts` 移除 `tailwindcss()` 插件
- `src/index.css`：`@import "tailwindcss"` → `@tailwind base/components/utilities`

#### 验证

| 特性 | v4 产物 | v3 产物 | 门槛 |
|---|---|---|---|
| `@layer` | 5 | **0** | Chrome 99 |
| `@property` | 45 | **0** | Chrome 85 |
| `oklch(` | 27 | **0** | Chrome 111 |
| `color-mix(` | 2 | **0** | Chrome 111 |
| `:where()` | 9 | 5（仅 preflight，**工具类区为 0**） | Chrome 88 |

CSS 由 20.62 kB 降至 15.12 kB。构建通过，测试 13/13 通过。

> **注意**：`@vitejs/plugin-legacy` 只处理 JS、**不处理 CSS**，对这类问题无效。

---

### 三、修复部署后移动端长期拿到旧版本（PR #6 · `c8aec99`）

#### 现象

上线第二期修复后，**夸克正常了，百度 App 仍然无样式**。

#### 排查与根因

同一份服务器文件、同一台手机，两个浏览器表现不同——这直接排除了代码问题，指向客户端。查日志，看部署后百度 App 的**全部**请求：

```
百度App  03:03:28  GET /api/todos      200 813
百度App  03:03:28  GET /api/sessions   200 794
百度App  03:05:15  GET /api/todos      200 813
百度App  03:05:15  GET /api/sessions   200 794
```

**没有一条 `GET /`，没有一条 `/assets/*`。** 对比夸克：

```
夸克  03:04:01  GET /                          200    396
夸克  03:04:01  GET /assets/index-M9EgBFvv.css 200  15116
夸克  03:04:01  GET /assets/index-01YtoMAf.js  200 160289
```

结论：**百度 App 把整个页面（HTML + CSS + JS）都缓存了**，跑的是缓存里的旧 Tailwind v4 CSS，第二期的修复**根本没送达**。API 返回 200 说明带着有效 JWT、JS 运行正常，只是样式是旧的。

根因在 nginx：

```
$ curl -sI http://47.106.200.121:8310/
HTTP/1.1 200 OK
Content-Type: text/html
Last-Modified: …
ETag: "6aab5784-18c"
                        ← 没有 Cache-Control
```

`nginx.conf` 未设置任何 `Cache-Control`，浏览器便自行套用启发式缓存，各家用多激进全凭自己：夸克还算克制会回源，百度 App 则长期不校验。

**用 `?v=2` 绕过缓存验证**（缓存按完整 URL 做键，新键必然未命中）：

```
03:09:31  GET /?v=2                        200    396
03:09:31  GET /assets/index-M9EgBFvv.css   200  15116   ← 新 CSS
03:09:31  GET /assets/index-01YtoMAf.js    200 160289   ← 新 JS
03:09:31  GET /favicon.ico                 200    396   ← 连图标都重取
```

`favicon.ico` 也被重新请求，是「真正全新加载」的铁指纹。至此缓存假设确认。

#### 改动

```nginx
# 构建产物（文件名含内容哈希，内容变则文件名变）：可永久缓存
location /assets/ {
    add_header Cache-Control "public, max-age=31536000, immutable";
}

# 入口 HTML：每次回源校验（命中 ETag 返回 304，开销可忽略）
location / {
    try_files $uri $uri/ /index.html;
    add_header Cache-Control "no-cache";
}
```

标准 SPA 配法：**入口不缓存 + 哈希资源永久缓存**。`index.html` 仅 396 字节，每次 304 校验成本可忽略；带哈希的资源文件名随内容变，缓存永不冲突。

#### 验证

| 检查项 | 结果 |
|---|---|
| 入口 HTML `Cache-Control` | `no-cache` |
| 哈希资源 `Cache-Control` | `public, max-age=31536000, immutable` |
| 条件请求（`If-None-Match`） | **HTTP 304** |
| API 反代 | 401（正常，空 body 被拒） |

部署流程改进：替换线上配置前，先用共享网络的一次性容器跑 `nginx -t` 校验语法，通过后才替换。

---

### 四、待办事项支持合集 / 子集（PR #10）

#### 需求

一个待办事项可能是几个的合集，例如「408」包含 4 门课。人不可能同时在 408 和操作系统上分别计时，于是希望：

- 一个事项可以加若干**子集**，子集可以**独立计时**
- 子集**没有等级和荣誉系统**
- 子集时长之和 = 该事项的总时长，等级/荣誉按这个总时长算
- 有了子集的**事项自身取消计时功能**
- **不加子集的事项行为完全不变**

#### 数据模型

只加两个可空列，`ddl-auto: update` 会自动迁移（MySQL 8 的 `ADD COLUMN` 对可空列是 INSTANT 操作，不重建表）：

| 列 | 含义 |
|---|---|
| `todos.parent_id` | 所属合集 id；null 表示顶层事项。**只允许一层嵌套** |
| `time_sessions.root_subject` | 开计时时快照的合集名；null 表示不属于任何合集 |

`root_subject` 是必需的：计时记录**故意在待办被删后保留**（见下），统计因此无法反查待办表，只能在记录里快照一份合集名。

#### 关键设计

**1. 统计的合并 / 展开**

聚合键取 `rootSubject ?? subject`：

| 视图 | 聚合键 | 效果 |
|---|---|---|
| 合并（默认） | `rootSubject ?? subject` | 408 及其 4 门课合成一根柱子 |
| 展开 | `subject` | 4 门课各自成柱 |

没有子集时开关**不显示**，界面与改动前一致。层级聚合全部在**前端**完成（`time.ts` 的 `buildStats`），后端不新增任何计算。

**2. 删除语义：删事项，留记录**

`TimeSession` 的注释写着「删除待办后保留历史，用于统计」，这是原有约定。合集沿用同一条：**级联删掉子集事项与计时记录无关**——事项删了，计时记录仍然保留。

`clearCompleted` 需要特别处理：合集已完成时它的子集会被级联删除，遍历时必须跳过这些子集，否则会重复删除。

**3. 历史时间迁移**

给一个已经计过时的事项加第一个子集时，前端弹出提示，让用户把已有记录**手动迁到某个子集**（`POST /api/todos/{id}/adopt-time`），也可以选「暂不迁移」。迁移会改写记录的 `todoId` / `subject` / `rootSubject`。

**4. 单计时器规则不变**

`TimeSessionService.start()` 仍会自动关掉其它正在跑的计时器——**同一时刻只有一个计时器**。这条对子集同样生效，因此不能同时给「数据结构」和「操作系统」计时。后端额外拒绝给合集计时，并在创建子集时结束合集可能仍在跑的会话，保证「合集不存在运行中的会话」这个不变量。

#### 验证

- 后端 `mvn compile` 通过
- 前端构建通过
- 测试 **17/17** 通过（新增 4 条：合集自身无计时、子集无等级、删除级联、迁移提示）

---

### 五、移除「清零」计时功能（PR #13 · `dae2f9e`）

#### 动机

累计时长类应用不该提供把已有记录一键抹掉的入口。

#### 改动

| 层 | 删除内容 |
|---|---|
| 后端 | `POST /api/todos/{id}/reset` 端点、`TimeSessionService.reset()`、`TimeSessionRepository.deleteByUserIdAndTodoId`（仅被 reset 调用，留着就是孤儿） |
| 前端 | `api.resetTime`、`store.resetTodoTime`（声明 / 实现 / 导出）、`TodoList` 的清零按钮与 `onReset` prop |
| 测试 | 对应 mock 与断言（计时 / 暂停用例本身保留） |
| 文档 | `README.md` 与 `CLAUDE.md` 的 session 模块说明改为「开始 / 暂停 / 迁移时长」 |

共 8 文件、**+11 / −66**，纯删除。

#### 影响

- **存量数据不受影响**：reset 原本只是删 `time_session` 记录，删掉接口不会碰任何已有数据
- **不可逆**：此后界面上不再有任何删除计时记录的入口，时长只能通过开始 / 暂停累积

#### 验证

- 前端构建通过，测试 **17/17** 通过
- 后端 28 个源文件编译通过
- 全仓库 `清零` / `resetTime` / `resetTodoTime` / `onReset` / `deleteByUserIdAndTodoId` 零残留
- 线上 JS 中 `清零` 与 `resetTime` 均为 0 次

> 后端端点的删除无法在未登录状态下验证 —— Spring Security 在进入路由前就返回 401，区分不出「未登录」和「端点不存在」。该端点靠源码零残留 + 编译通过保证。

---

## 环境约束备忘

1. **Tailwind 必须保持 v3，不要升级 v4。** 决定因素不是「新不新」而是**目标用户的浏览器内核**。百度 App 的 T7 内核为 Chromium 97，不支持 `@layer`。升级 v4 的前提是所有目标浏览器 ≥ Chrome 99。自检方法：构建后检查 `dist/assets/*.css` 中 `@layer` 计数，v4 会 > 0。

2. **部署后若用户反馈「还是旧版本」，先怀疑缓存。** 读 nginx 访问日志即可判定：若只有 `/api/*` 没有 `/assets/*`，就是缓存未刷新，让用户清一次缓存即可（`index.html` 现已为 `no-cache`，此为一次性操作）。

3. **排查移动端问题优先读访问日志，不要停留在推断。** 本次两次关键定位（内核版本、缓存未刷新）都来自日志中的 UA 与请求序列。日志里没有的结论，都只是假设。

4. `@vitejs/plugin-legacy` 只降级 JS，不处理 CSS，无法解决 `@layer` 这类问题。

5. **中文字体只能自托管，且「免费商用」≠「可用于网页」。** Android 只带思源黑体，`font-family` 调不到任何书法字体，所以换字体必须自托管文件。挑字体时按授权分三档看：

   | 档 | 协议 | 能做什么 |
   |---|---|---|
   | **OFL**（霞鹜文楷、马善政、得意黑等） | SIL Open Font License 1.1 | 可修改、**可子集化**、可嵌入、可再分发。最省心 |
   | **作者声明「免费商用」**（云峰飞云体、平方系列、庞门正道等） | 各家自定 | 通常**明文禁止修改** —— 于是**不能子集化**，只能整包引入；嵌入一项常有留白 |
   | **付费**（优设书华体等） | 商业授权 | 本项目不用 |

   实际影响：OFL 字体子集化后约 10 KB；不能改的毛笔体整包 3~25 MB。**给项目挑字体时，先确认授权是否允许修改，再谈好不好看** —— 顺序反了会白挑一轮。

6. **`ssh-todo.sh --put <目录> <远程目录>` 是把「目录本身」解到远程目录下**，不是把目录内容摊进去。传 `dist` 恰好符合直觉（落到 `<远程目录>/dist`），但**传单文件会落到 `<远程目录>/<本地目录名>/` 下面**，原文件不动。传单文件的正确姿势：上传到暂存目录 → 远端 `mv` 就位 → **`md5sum` 核对**。只看「命令没报错」会被骗过去 —— 容器照常重建、站点照常可访问，只有配置没变。

7. **用 `docker exec ... mysql` 灌含中文的数据，必须显式带 `--default-character-set=utf8mb4`，否则中文会双重编码。** 2026-09-18 给 admin 灌称号示例时踩到，页面上显示成 `ç§°å·ç¤ºä¾‹ Â· æ–°æ‰‹`。根因是一条链：

   - MySQL 官方镜像**不设置 locale** —— 容器里 `LANG` 为空、`LC_CTYPE=POSIX`
   - `mysql` 客户端的 `default-character-set` 默认值是 **`auto`**，含义是「按 OS locale 猜」
   - POSIX locale 下猜出来的结果是 **latin1**

   于是 UTF-8 的 SQL 文件被按 latin1 解释：字节 `E7 A7 B0`（「称」）读成 latin1 的 `ç§°`，再按 utf8mb4 存成 `C3A7 C2A7 C2B0` —— 每个字节各自升格成一个字符。这就是双重编码。

   **为什么应用没事**：后端走 JDBC（`characterEncoding=utf8`），链路里根本没有 locale 这一环。所以这类乱码**只会出在手工灌数据时**，不会出在应用写入的数据上（实测对照：同一张表里应用写入的中文 hex 是 `E7A4BA…`，正常）。

   **一个极具迷惑性的现象**：用 `docker exec mysql` 查回来**看着是正常中文**。因为显示路径上又做了一次方向相反的转换（utf8mb4 → latin1 输出 → 终端按 UTF-8 解释），两次错误恰好抵消。**唯一的硬证据是 `hex()`，不是肉眼看着对不对**：

   ```sql
   select hex(text) from todos where ...;
   -- 正常 UTF-8 中文以 E4~E9 开头；双重编码以 C3 开头（ç = C3A7）
   ```

   正确灌法（多一个参数就对了）：

   ```bash
   docker exec -i todo-mysql mysql --default-character-set=utf8mb4 -utodo -p"$MYSQL_PASSWORD" todo < seed.sql
   ```

   **修法用「删掉重灌」，不用 in-place UPDATE。** 2026-09-18 实修走的就是这条路，已验证：按第十七期那两行 `delete` 清掉，再用上面那条带 `--default-character-set=utf8mb4` 的命令重灌同一份 `seed.sql`。双重编码理论上可逆（每个源字节都落在 U+0000~U+00FF 内，`update ... set text = convert(binary convert(text using latin1) using utf8mb4)` 应该能转回来），**但这条 UPDATE 我没实测过**，所以没有采用 —— 坏数据只是示例，重灌比赌一条没验过的转换更直接。

   重灌前**核对种子文件的 md5**（本地 vs 服务器）。少了这一步，万一上传途中文件坏掉，修完还是乱码，会误判成「方法不对」再查一轮。

8. **gzip 不生效多半是 `Content-Type` 没对上，而不是 `gzip_types` 写错了。** `gzip_types` 按响应头的 `Content-Type` 匹配；nginx 自带 `mime.types` **没有 `.ttf` 条目**，`.ttf` 会落到 `default_type`（`application/octet-stream`）。验证方式：`curl -H 'Accept-Encoding: gzip' -o /dev/null -w '%{size_download}'` 对比加不加该头的大小，别只看配置里有没有写 `gzip on`。

9. **`desktop/` 的 Electron 打包在国内环境有两个固定故障点，都不在代码里。**（2026-09-18 首次打包 exe 时全踩到）

   - **`npm install` 崩在 electron 的 postinstall**，退出码 `3221226505`，报错信息读不出原因。electron 与 electron-builder 的二进制都不在 npm 上，是 postinstall 里从 **GitHub Releases** 拉的，国内直连被掐。`desktop/.npmrc` 里已落 npmmirror 镜像（那两个键恰好就是 electron 的 `install.js` 与 electron-builder 的 `binDownload.js` 要读的环境变量名），**照常 `npm install` 即可**。
     - ⚠️ 验证镜像有没有生效，必须**先清空 `%LOCALAPPDATA%\electron\Cache` 且不设任何环境变量** —— 本地装成功过一次之后，缓存会让后续任何配置都「看起来有效」。
   - **electron-builder 解压 `winCodeSign` 失败**：包里含 macOS 的 `.dylib` **符号链接**，Windows 建符号链接要管理员权限或开发者模式，于是报「客户端没有所需的特权」并重试 4 次后放弃。绕过：用包内的 `7za.exe` 手工解压并 `-x!darwin` 跳过符号链接，产物放到 electron-builder 期望的缓存位置 `%LOCALAPPDATA%\electron-builder\Cache\winCodeSign\winCodeSign-2.6.0\`。
     - 缓存目录名是 `<name>-<version>`，**别被一堆纯数字目录骗了** —— 那些是解压失败后残留的临时目录，electron-builder 不会去那里找。
     - **这个缓存不能省**：给 exe 写图标与版本信息的 `rcedit-x64.exe` 就在包里，缺了它只能打出没有图标、没有版本信息的 exe。

---

## 部署信息

- 地址：http://47.106.200.121:8310/
- 目录：`/root/todo/`
- 服务：`todo-mysql`（MySQL 8）、`todo-backend`（JRE 21）、`todo-app`（nginx，反代 `/api` → backend:8080）
- 端口：仅 8310 对外

服务器目录与仓库**同构**（2026-09-18 起）：

```
/root/todo/
├── frontend/{Dockerfile, nginx.conf, dist/}   # 前端构建上下文
├── backend/
├── docker-compose.yml                          # frontend 的 build.context = ./frontend
└── .env                                        # 只存在于服务器，绝不提交
```

**部署流程**：先清空远程 `frontend/dist`。上传是解包覆盖、只增不删，不清就会持续累积历次构建的哈希文件 —— 2026-09-18 一次性清掉 4 个共约 197 KB（`index-BMaBm_re.js`、`index-x0-7uTL0.css`、`Stats-CSlg7KrG.js`、`smiley-subset-Cg7bpxhD.woff2`）。此后固定为：

```
cd frontend && npm run build
→ rm -rf /root/todo/frontend/dist      # 先清空，见下
→ 上传 frontend/dist、frontend/nginx.conf、docker-compose.yml
→ 校验 nginx 语法
→ docker compose up -d --build --force-recreate frontend
```

**`docker-compose.yml` 必须一起上传** —— 这是整理目录时暴露的漏洞：此前部署流程从不上传它，服务器上那份从 9-15 起就没动过。当时核对下来只是行尾（CRLF vs LF）差异、内容一致，所以一直没出事；但只要仓库里真改了它（比如这次改 `build.context`），不上传就会「仓库改了、服务器没改」，下次构建直接挂。

清空是**安全的**：frontend 服务没挂 volume，`Dockerfile` 是 `COPY dist/`，线上容器读的是**镜像里自带的那份**，`/root/todo/frontend/dist` 只是构建原料 —— 动它不影响正在跑的站点，不存在「清空瞬间访客 404」的窗口（这一点 2026-09-18 之前我判断错了，当时以为有风险）。

上传后要比对一次文件清单 / md5（本地与远程 `dist` 应完全一致），否则上传中断会留下一份残缺的 dist，被下一次构建烤进镜像。比对本身有三个坑，2026-09-18 实跑时全踩到了：两边都要 `LC_ALL=C sort`，否则本地 locale 与远程字节序不同、排序不一致会**误报差异**（`Stats-*.js` 里的大写 `S` 就会错位）；比 md5 还要归一化 `md5sum` 的分隔符（本地 git-bash 是二进制模式 `hash *file`，远程是 `hash␣␣file`），否则 7 个文件哈希全同也会整片报差异；**比任何文本文件前两边都要去掉 `\r`** —— `core.autocrlf=true` 让工作区是 CRLF 而仓库里是 LF，服务器那份又取决于当初怎么传的，不归一化就会像配置漂移（`nginx.conf` 与 `docker-compose.yml` 各中过一次，见第十二期）。

（若哪天想手工核对哪些文件还在用：以 `dist/index.html` 为起点顺藤摸瓜 —— 入口 html 给出 js/css，入口 js 给出懒加载分块如 `Stats-*.js`，入口 css 给出版本化字体。）

**删这些文件不影响回滚。** 回滚靠的是 `docker images -f dangling=true` 里那批悬空镜像 —— `Dockerfile` 是 `COPY dist/`，每个镜像**自带**构建当时的一整套文件，不读 `/root/todo/frontend/dist`（已实测：上一个版本的镜像 `0c187792b9e9` 里仍完整保有它那一版的 `index-BMaBm_re.js`、`smiley-subset-Cg7bpxhD.woff2` 等）。所以只要不 `docker image prune`，任何一版都能直接 `docker run` 起来。

**镜像保留策略：只保留最近 3 个悬空镜像（= 前 3 个版本可回滚），更老的删掉。** frontend 与 backend 各自算一份；当前在跑的 `todo-app:latest` / `todo-backend:latest` 是 tagged 的，天然不在清理范围内。

- 做法：按 `com.docker.compose.project=todo` + `com.docker.compose.service` 过滤悬空镜像，按创建时间倒序保留最近 3 个，其余**逐个 `docker rmi <id>`，不加 `-f`** —— 被容器占用的镜像 Docker 会自己拒绝删除，这是安全网。
- **绝不使用 `docker image prune` / `docker system prune`**：那是全局命令，会牵连本机其它项目。这台服务器上还跑着 `redis1/2/3`（`redis:8.0-alpine`，已开机 15 个月）和 `mysql`，它们的镜像必须原样保留。
- **别按镜像标称大小估算收益。** frontend 镜像之间几乎全部层共享，各版独有的只有 `dist` 那一个 COPY 层。2026-09-18 首次执行：镜像数 30 → 14，删掉 16 个（0 失败），但 `docker system df` 里 Images 的 SIZE 几乎没动（1.443GB → 1.44GB），实际只释放约 3MB。**这条规则的价值是「回滚集有界、看着清爽」，不是省磁盘** —— 当时残留的 519MB 可回收空间主要来自未使用的**基础镜像**（`redis:latest` 128MB 等）与 build cache，与版本累积无关，且不属于本项目、未动。
