# electron 与 electron-builder 的二进制都不在 npm 上，装在 postinstall 里从 GitHub Releases 拉。
# 国内直连会在这一步崩掉（exit code 3221226505，连报错都读不出原因），换成 npmmirror 镜像。
#
# 这两个键会变成 npm_config_electron_mirror / npm_config_electron_builder_binaries_mirror，
# 正好是 electron 的 install.js 与 electron-builder 的 binDownload.js 要读的环境变量名。
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
